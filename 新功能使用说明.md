# 🎉 新功能使用说明

## ✅ 已完成的功能

### 1. 知识库切换高亮 ✨
**问题：** 切换知识库后，选中状态不显示  
**解决：** 添加了本地状态管理`currentKBId`，切换后立即更新高亮显示

**效果：**
- 当前知识库显示蓝色边框和背景
- 显示"当前"标签
- 其他知识库显示灰色边框

### 2. 知识库文件列表 📁
**位置：** 知识库管理面板底部  
**功能：**
- 自动显示当前知识库的所有文件
- 显示文件名、大小、类型
- 不同文件类型显示不同图标
- 可以折叠/展开列表

### 3. 删除文件功能 🗑️
**操作：**
1. 在文件列表中，鼠标悬停文件
2. 点击右侧出现的垃圾桶图标
3. 确认删除

**效果：**
- 删除原文件（data/{kb_id}/{filename}）
- 删除向量记录（vector_db/{kb_id}/中的对应记录）
- 显示删除的记录数量

### 4. 文档查看器知识库选择 🔍
**位置：** 文档查看器左侧面板顶部  
**功能：**
- 下拉菜单选择任意知识库
- 自动加载该知识库的文件列表
- 切换知识库后文档列表实时更新

### 5. 在线查看文档（不下载） 👀
**问题：** 点击文档直接下载  
**解决：** 修改后端返回`Content-Disposition: inline`

**现在支持：**
- ✅ PDF - 直接在浏览器预览
- ✅ 图片 - 直接显示
- ✅ TXT/MD - 文本查看
- ✅ DOCX - 转HTML预览
- ✅ PPTX - 文本内容显示

---

## 🚀 使用指南

### 知识库管理面板

#### 打开方式
点击左侧边栏的 **数据库图标** (Database)

#### 面板功能布局
```
┌─────────────────────────────────────────┐
│ 🗄️ 知识库管理                   ✕     │
├─────────────────────────────────────────┤
│ [+ 创建新知识库]                        │
├─────────────────────────────────────────┤
│ 📤 上传文件到当前知识库                  │
│ [选择文件区域]                          │
├─────────────────────────────────────────┤
│ 📚 已有知识库                           │
│ ┌─────────────────────────────────┐    │
│ │ ■ 默认知识库        [当前]      │    │
│ │   大小: 1.45 MB                 │    │
│ └─────────────────────────────────┘    │
│ ┌─────────────────────────────────┐    │
│ │ □ physics           [切换] [删除]│    │
│ │   大小: 0.85 MB                 │    │
│ └─────────────────────────────────┘    │
├─────────────────────────────────────────┤
│ 📄 当前知识库文件 (3)          [v]     │
│ ┌─────────────────────────────────┐    │
│ │ 📕 OS-01.pdf          [删除]     │    │
│ │   1.2 MB • PDF                   │    │
│ ├─────────────────────────────────┤    │
│ │ 📘 notes.txt          [删除]     │    │
│ │   0.05 MB • TXT                  │    │
│ └─────────────────────────────────┘    │
└─────────────────────────────────────────┘
```

#### 操作流程

**1. 创建新知识库**
```
1. 点击 [+ 创建新知识库]
2. 输入名称（如：physics、programming）
3. 点击 [确认创建]
4. 系统自动创建：
   - data/physics/
   - vector_db/physics/
```

**2. 上传文件**
```
1. 确保当前知识库被选中（显示"当前"标签）
2. 点击 [选择文件] 或拖拽文件
3. 选择PDF、DOCX、TXT等文件
4. 点击 [上传并处理]
5. 等待处理完成
6. 文件自动出现在下方文件列表中
```

**3. 切换知识库**
```
1. 找到目标知识库
2. 点击 [对钩图标]
3. 当前标签移动到该知识库
4. 下方文件列表自动更新
```

**4. 查看文件列表**
```
1. 文件列表默认展开
2. 点击标题可折叠/展开
3. 显示所有文件的：
   - 文件名
   - 文件大小
   - 文件类型
```

**5. 删除文件**
```
1. 鼠标悬停在文件上
2. 右侧出现 [删除] 按钮
3. 点击并确认
4. 同时删除：
   - 原文件（data/{kb_id}/{filename}）
   - 向量记录（vector_db/{kb_id}/中的记录）
```

**6. 删除知识库**
```
1. 找到要删除的知识库（不能是默认知识库）
2. 点击 [垃圾桶图标]
3. 确认删除
4. 系统删除整个目录：
   - data/{kb_id}/
   - vector_db/{kb_id}/
```

---

### 文档查看器

#### 打开方式
1. 点击侧边栏的 **文档查看图标**
2. 从对话引用中点击 **查看文档**
3. 从文档大纲/概念定位跳转

#### 新增功能：知识库选择

**位置：** 左侧面板顶部

```
┌─────────────────────────────────────┐
│ 🗄️ 文档列表                         │
│                                     │
│ 选择知识库                          │
│ [默认知识库        ▼]              │
│                                     │
│ 3 个文档                            │
├─────────────────────────────────────┤
│ □ OS-01.pdf                        │
│ □ OS-02.pdf                        │
│ ■ notes.txt                        │
└─────────────────────────────────────┘
```

**使用方法：**
```
1. 打开文档查看器
2. 点击 [选择知识库] 下拉菜单
3. 选择任意知识库（如：physics）
4. 左侧文档列表自动更新为该知识库的文件
5. 点击文件即可查看
```

**文档URL说明：**
- 旧路径：`/api/documents/{filename}/raw`
- 新路径：`/api/knowledge-bases/{kb_id}/documents/{filename}/raw`
- 自动根据选中的知识库加载对应文件

#### 在线查看效果

**PDF文件：**
- ✅ 浏览器内直接显示
- ✅ 可以缩放、翻页
- ✅ 可以选中和复制文本
- ❌ 不会触发下载

**图片文件：**
- ✅ 直接显示
- ✅ 支持缩放（10%-500%）
- ✅ 深色背景

**文本文件（TXT/MD）：**
- ✅ 格式化显示
- ✅ 保留换行和缩进
- ✅ 可以滚动和复制

**DOCX文件：**
- ✅ 转为HTML在线显示
- ✅ 保留基本格式
- ✅ 支持表格

**PPTX文件：**
- ✅ 显示文本内容
- ✅ 支持分页
- ⚠️ 不显示图片和动画

---

## 🔧 技术实现

### 后端新增API

#### 1. 获取知识库文件列表
```http
GET /api/knowledge-bases/{kb_id}/files

Response:
{
  "success": true,
  "kb_id": "default",
  "files": [
    {
      "name": "OS-01.pdf",
      "size": 1.23,
      "type": "pdf",
      "created_at": 1234567890
    }
  ],
  "count": 1
}
```

#### 2. 删除知识库文件
```http
DELETE /api/knowledge-bases/{kb_id}/files/{filename}

Response:
{
  "success": true,
  "message": "文件 'OS-01.pdf' 已删除",
  "kb_id": "default",
  "filename": "OS-01.pdf",
  "vector_records_deleted": 15
}
```

#### 3. 获取知识库文档（在线查看）
```http
GET /api/knowledge-bases/{kb_id}/documents/{filename}/raw

Response: [文件内容]
Headers:
  Content-Type: application/pdf
  Content-Disposition: inline; filename="OS-01.pdf"
```

#### 4. DOCX预览
```http
GET /api/knowledge-bases/{kb_id}/documents/{filename}/preview/docx

Response: [HTML内容]
```

#### 5. PPTX预览
```http
GET /api/knowledge-bases/{kb_id}/documents/{filename}/preview/pptx?page=1

Response: [HTML内容]
```

### 前端组件更新

#### KnowledgeBasePanel.jsx
**新增功能：**
- `currentKBId` 状态管理当前知识库
- `currentKBFiles` 存储当前知识库的文件列表
- `loadKBFiles()` 加载文件列表
- `handleDeleteFile()` 删除文件
- 文件列表UI组件
- 切换时自动更新高亮

#### DocumentViewer.jsx
**新增功能：**
- `knowledgeBases` 存储所有知识库
- `selectedKB` 当前选中的知识库
- `loadKnowledgeBases()` 加载知识库列表
- 知识库选择下拉菜单
- 根据选中知识库加载文件
- API路径自动适配知识库

### 文件路径规则

**原文件存储：**
```
data/
  ├── default/           # 默认知识库
  │   ├── OS-01.pdf
  │   └── notes.txt
  └── physics/           # physics知识库
      └── physics-book.pdf
```

**向量库存储：**
```
vector_db/
  ├── default/           # 默认知识库向量
  │   └── chroma.sqlite3
  └── physics/           # physics知识库向量
      └── chroma.sqlite3
```

**访问URL：**
```
# 访问default知识库的文件
/api/knowledge-bases/default/documents/OS-01.pdf/raw

# 访问physics知识库的文件
/api/knowledge-bases/physics/documents/physics-book.pdf/raw
```

---

## 📊 功能对比

### 之前 vs 现在

| 功能 | 之前 | 现在 |
|------|------|------|
| 知识库切换高亮 | ❌ 不显示 | ✅ 蓝色高亮+标签 |
| 查看知识库文件 | ❌ 无法查看 | ✅ 完整文件列表 |
| 删除文件 | ❌ 只能手动删 | ✅ UI界面删除 |
| 文档查看器选择库 | ❌ 只看data/ | ✅ 选择任意知识库 |
| 文档在线查看 | ❌ 自动下载 | ✅ 浏览器内查看 |
| 文件路径管理 | 混乱 | data/{kb_id}/ 清晰 |

---

## 🐛 已修复的问题

### 1. 知识库切换不高亮
**原因：** 没有本地状态管理  
**解决：** 添加`currentKBId`状态，切换时立即更新

### 2. 点击文档直接下载
**原因：** `Content-Disposition: attachment`  
**解决：** 改为`Content-Disposition: inline`

### 3. 无法查看知识库文件
**原因：** 缺少API  
**解决：** 新增`/api/knowledge-bases/{kb_id}/files` API

### 4. 删除文件只删原文
**原因：** 未处理向量记录  
**解决：** 同时删除向量记录，返回删除数量

### 5. 文档查看器只能看一个库
**原因：** 硬编码读取data/目录  
**解决：** 添加知识库选择，动态加载

---

## 🎯 使用场景示例

### 场景1：创建专业知识库
```
1. 我想创建一个"物理课程"知识库
2. 点击 [创建新知识库]
3. 输入 "physics"
4. 上传 力学.pdf、电磁学.pdf
5. 系统处理并显示文件列表
6. 在文档查看器中选择"physics"查看
```

### 场景2：管理文件
```
1. 打开知识库管理面板
2. 切换到"physics"知识库（显示蓝色高亮）
3. 查看文件列表（3个文件）
4. 发现错误上传的文件
5. 悬停并点击 [删除]
6. 文件和向量记录都被删除
7. 列表实时更新
```

### 场景3：跨库查看文档
```
1. 打开文档查看器
2. 当前显示"default"库的文件
3. 点击下拉菜单
4. 选择"physics"
5. 左侧文档列表自动更新
6. 点击"力学.pdf"
7. PDF在浏览器中直接显示（不下载）
8. 可以缩放、复制文本
```

### 场景4：删除整个知识库
```
1. "physics"知识库不再需要
2. 在知识库列表中找到它
3. 点击 [垃圾桶图标]
4. 确认删除
5. 系统删除：
   - data/physics/ 目录
   - vector_db/physics/ 目录
6. 列表中移除该知识库
```

---

## ⚙️ 配置说明

### config.py 更新

```python
# 新增辅助函数
def get_kb_data_dir(kb_id="default"):
    """获取指定知识库的原文件目录"""
    return os.path.join(DATA_DIR, kb_id)

def get_kb_vector_dir(kb_id="default"):
    """获取指定知识库的向量库目录"""
    return os.path.join(VECTOR_DB_DIR, kb_id)

def ensure_kb_dirs(kb_id="default"):
    """确保知识库目录存在"""
    data_dir = get_kb_data_dir(kb_id)
    vector_dir = get_kb_vector_dir(kb_id)
    os.makedirs(data_dir, exist_ok=True)
    os.makedirs(vector_dir, exist_ok=True)
    return data_dir, vector_dir
```

---

## 📚 下一步可能的改进

### 1. 批量操作
- 批量选择文件
- 批量删除
- 批量移动到其他知识库

### 2. 文件预览增强
- 使用react-doc-viewer库
- 支持更多格式
- 添加全文搜索高亮

### 3. 知识库统计
- 文件数量统计
- 向量数量统计
- 存储空间统计

### 4. 文件导出
- 导出整个知识库
- 导出为ZIP
- 备份和恢复

### 5. 权限管理
- 不同知识库设置不同权限
- 只读/读写权限
- 共享知识库

---

## ✨ 总结

所有请求的功能都已实现：

1. ✅ **知识库切换高亮** - 当前库显示蓝色边框和标签
2. ✅ **文件列表显示** - 在知识库面板底部展示
3. ✅ **删除文件功能** - 同时删除原文件和向量记录
4. ✅ **文档查看器选择库** - 下拉菜单选择任意知识库
5. ✅ **在线查看文档** - 不再自动下载，浏览器内查看

**现在您可以：**
- 在多个知识库间自由切换
- 清晰看到每个库的文件
- 方便地管理和删除文件
- 跨知识库查看文档
- 在浏览器中直接查看各种文档

**重启后端测试：**
```bash
cd /home/honglianglu/hdd/rag-agent
./start_backend.sh
```

刷新前端页面，所有新功能即可使用！🎉



