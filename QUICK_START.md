# 🚀 快速启动指南

## 启动步骤

### 1. 启动后端
```bash
cd /home/honglianglu/hdd/rag-agent
python backend/api.py
```

等待看到：
```
✅ RAG Agent 初始化成功
✅ API Reranker 加载成功: Pro/BAAI/bge-reranker-v2-m3
✅ BM25 index built successfully
INFO:     Uvicorn running on http://0.0.0.0:8000
```

### 2. 启动前端
```bash
cd /home/honglianglu/hdd/rag-agent/frontend
npm install  # 首次运行需要
npm start
```

等待看到：
```
Compiled successfully!
Local: http://localhost:3000
```

### 3. 打开浏览器
访问：http://localhost:3000

---

## 🎯 体验新功能

### 功能1：实时检索结果
1. 输入问题："什么是虚拟内存？"
2. 点击发送
3. **立即看到**：检索结果卡片（带截图）
4. 等待2-5秒后看到AI答案

### 功能2：可点击引用
1. 在AI答案中找到蓝色的引用按钮
2. 点击引用
3. **自动打开**：右侧文档查看器
4. **自动跳转**：到对应页码

### 功能3：图片预览
1. 查看检索结果卡片
2. **直接看到**：文档页面截图
3. 点击卡片查看完整文档

---

## 🔍 测试建议

### 测试问题
```
1. "什么是虚拟内存？"
2. "解释一下页表的作用"
3. "进程和线程的区别是什么？"
```

### 观察要点
- ✅ 检索结果是否在2秒内显示
- ✅ 是否显示文档截图
- ✅ 答案中是否有蓝色引用按钮
- ✅ 点击引用是否跳转到正确页码
- ✅ ToolPanel中是否显示图片

---

## 🐛 常见问题

### 问题1：图片不显示
**原因**：静态文件目录不存在  
**解决**：
```bash
mkdir -p /home/honglianglu/hdd/rag-agent/backend/static/doc_images
```

### 问题2：引用点击无反应
**原因**：cite_id格式不匹配  
**检查**：后端日志中的[ID:xxx]和前端<cite id="xxx">是否一致

### 问题3：流式API报错
**原因**：前端请求地址错误  
**检查**：`useChat.js`中的URL是否为`http://localhost:8000/api/chat/stream`

---

## 📊 预期效果

### 时间线
```
0s    - 用户点击发送
0.5s  - 显示"正在检索..."
1-2s  - 显示检索结果卡片（3-5个）
3-5s  - 显示AI答案（带引用链接）
5s    - 检索结果卡片淡出
```

### 日志输出
```
========================================
🔎 开始增强检索 (SOTA)
========================================
  🔍 查询增强: ['虚拟内存', '页表', 'page table']
  📥 粗排召回: Vector=12, BM25=8
  🔗 RRF融合: 18 条文档
  ⚡ API Rerank 精排中...
  ✅ Rerank完成: 10 个结果
  ✅ 最终选取 10 个高质量片段
```

---

## 🎉 成功标志

当你看到以下现象时，说明所有功能正常：

1. ✅ 提问后立即看到检索结果卡片
2. ✅ 卡片中显示文档截图
3. ✅ 答案中有蓝色的引用按钮（不是"材料1"）
4. ✅ 点击引用能跳转到文档查看器
5. ✅ 文档查看器自动定位到正确页码

---

**祝使用愉快！** 🎊



