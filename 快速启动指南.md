# 快速启动指南

## 🚀 启动步骤

### 1. 安装LibreOffice（强烈推荐）

LibreOffice用于自动将PPTX/DOCX转换为PDF，以获得最佳的OCR和图片提取效果。

**如果有sudo权限**：
```bash
cd /home/honglianglu/hdd/rag-agent
./安装LibreOffice.sh
```

**如果没有sudo权限（推荐）**：
```bash
cd /home/honglianglu/hdd/rag-agent
./安装LibreOffice_用户版.sh
```

这会在你的用户目录（`~/.local/libreoffice`）安装LibreOffice，无需sudo权限！

**安装后激活**：
```bash
# 当前终端临时生效
export PATH="$HOME/.local/bin:$PATH"

# 或重新加载配置（永久生效）
source ~/.bashrc
```

**如果不想安装**：
- 系统会自动使用基础解析器（仅提取文本）
- 详见：**无sudo权限使用指南.md**

---

### 2. 启动后端
```bash
./start_backend.sh
```

**预计时间**：5-10秒

**输出示例**：
```
==========================================
🚀 启动 FastAPI 后端
==========================================
📡 后端地址: http://localhost:8000
📖 API 文档: http://localhost:8000/docs

INFO:     Uvicorn running on http://0.0.0.0:8000
INFO:     Application startup complete
```

---

### 3. 启动前端（另一个终端）
```bash
./start_frontend.sh
```

**预计时间**：10-30秒

**输出示例**：
```
  VITE v5.x.x  ready in 1234 ms

  ➜  Local:   http://localhost:5173/
  ➜  Network: use --host to expose
```

---

### 4. 打开浏览器
```
http://localhost:5173
```

---

## ✅ 验证安装

### 测试1: 上传PDF（中文文件名）
1. 点击左侧栏 "数据库" 图标
2. 选择 "default" 知识库
3. 上传一个中文文件名的PDF
4. 应该成功，无编码错误 ✅

### 测试2: 上传PPTX
1. 上传一个PPTX文件
2. 后端日志应该显示：
   ```
   🔄 转换为PDF: test.pptx
   ✅ 转换成功: test.pdf
   ✅ MinerU处理完成
   ```
3. 文件列表显示新文件 ✅

### 测试3: 查看文档
1. 点击左侧栏 "文档查看" 图标
2. 选择任意PDF
3. 应该在线显示，不下载 ✅

---

## 🛑 停止服务

```bash
# 停止所有服务
./stop_dev.sh

# 或者分别停止
Ctrl+C  # 在运行后端/前端的终端中
```

---

## ⚠️ 常见问题

### Q1: 端口8000被占用
```bash
# 查找占用进程
lsof -ti :8000

# 终止进程
kill -9 $(lsof -ti :8000)

# 重新启动
./start_backend.sh
```

### Q2: 端口5173被占用
```bash
# 查找占用进程
lsof -ti :5173

# 终止进程
kill -9 $(lsof -ti :5173)

# 重新启动
./start_frontend.sh
```

### Q3: LibreOffice未安装
```bash
# 检查是否安装
which libreoffice

# 如果没有，运行安装脚本
./安装LibreOffice.sh
```

### Q4: 知识库切换失败
```bash
# 检查目录结构
ls -la data/
ls -la vector_db/

# 应该看到：
# data/default/, data/test/, ...
# vector_db/default/, vector_db/test/, ...
```

---

## 📖 更多文档

- **最新修复说明.md** - 所有最新的bug修复
- **目录结构修复说明.md** - 数据结构说明
- **UI功能完整指南.md** - 完整的UI功能
- **知识库管理指南.md** - 知识库管理

---

## 🎯 开始使用！

系统已经准备好了，开始：
1. 上传您的文档
2. 创建知识库
3. 开始提问

享受您的AI学习助手！🚀

