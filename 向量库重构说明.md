# 向量库生成流程重构说明

## 📋 重构内容

### 1. ✅ MinerU输出统一管理
- **位置**: `.mineru_output/` 目录
- **结构**: `.mineru_output/{文件名}/`
- **内容**: Markdown文本 + 提取的图片
- **优势**: 便于管理和复用，避免重复处理

### 2. ✅ 图片描述生成
**新增模块**: `image_describer.py`

**功能**:
- 使用多模态大模型（`Qwen/Qwen3-VL-32B-Instruct`）为图片生成描述
- 描述长度：50-150字
- 特点：客观准确、无幻觉、专业术语

**API调用**:
```python
describer = ImageDescriber()
description = describer.describe_image(
    image_path="path/to/image.png",
    context="图片周围的文字上下文"
)
```

**批量处理**:
```python
images_info = describer.batch_describe_images(
    images_info,  # 图片信息列表
    max_workers=3  # 并发数
)
```

### 3. ✅ 图片描述嵌入文本
**修改文件**: `document_loader.py`

**流程**:
1. MinerU提取图片
2. 为每张图片生成描述
3. 将描述插入到对应页面的文本中
4. 标注格式：
   ```
   【图片描述】(filename.png)
   这是一张展示虚拟内存页表结构的示意图...
   【/图片描述】
   ```

**优势**:
- 图片信息被文本化，可被文本向量库检索
- 清晰标注，便于后续引用
- 保留原图路径，可传给多模态模型

### 4. ✅ 动态TopK平衡
**修改文件**: `hybrid_retriever.py`

**策略**:
| 问题复杂度 | 文本初始检索 | 图片初始检索 | 最终返回 |
|-----------|------------|------------|---------|
| 简单 (≤5)  | 20个       | 5个        | 5个     |
| 中等 (≤10) | 50个       | 10个       | 10个    |
| 复杂 (>10) | 100个      | 20个       | 10+个   |

**实现**:
```python
retriever.search(
    query="用户问题",
    top_k=10,  # 最终返回数量
    use_dynamic_balance=True  # 启用动态平衡
)
```

**输出**:
```
📊 动态TopK: 文本=50, 图片=10 (最终返回10个)
```

### 5. ✅ Rerank模型集成
**新增模块**: `reranker.py`

**模型**: `Pro/BAAI/bge-reranker-v2-m3`

**流程**:
1. 初始检索：文本20-100个 + 图片5-20个
2. 合并结果
3. **Rerank重排序**：使用专业rerank模型评分
4. 返回top_k个最相关结果

**API调用**:
```python
reranker = Reranker()
reranked_docs = reranker.rerank(
    query="用户问题",
    documents=retrieved_docs,  # 检索到的文档
    top_k=10  # 返回前10个
)
```

**输出示例**:
```
🔄 Rerank: 对 60 个结果进行重排序...
✅ Rerank完成: 10 个结果
   Top3分数: ['0.892', '0.856', '0.734']
```

### 6. ✅ 智能模型选择
**修改文件**: `rag_agent.py`

**逻辑**:
```python
if 有图片输入 or context中有图片:
    使用多模态模型 (Qwen3-VL-32B)
    如果context中有图片，自动附加原图
else:
    使用纯文本模型 (Qwen2.5-72B)
```

**检测方法**:
- 检查retrieved_docs中是否有 `type='image'`
- 检查文本内容中是否有 `【图片描述】` 标记
- 检查是否有 `image_path` 字段

**优势**:
- 节省成本：纯文本用文本模型
- 提高质量：有图片时用多模态模型
- 自动化：无需手动指定

---

## 🔄 完整流程

### 文档上传流程
```
1. 用户上传文件 (PDF/PPTX/DOCX)
   ↓
2. 转换为PDF (如需要)
   ↓
3. MinerU处理
   - 输出到: .mineru_output/{文件名}/
   - 提取: 文本(Markdown) + 图片(PNG)
   ↓
4. 图片描述生成
   - 并发处理 (max_workers=3)
   - 使用多模态模型生成50-150字描述
   ↓
5. 图片描述嵌入文本
   - 插入到对应页面
   - 标注: 【图片描述】...【/图片描述】
   ↓
6. 文本分块
   - Chunk size: 500字符
   - Overlap: 100字符
   ↓
7. 向量化
   - 文本向量库: 文本内容(含图片描述)
   - 图片向量库: 图片CLIP向量
   ↓
8. 存储
   - 文本: vector_db/{kb_id}/course_documents
   - 图片: vector_db/{kb_id}/images/
```

### 查询流程
```
1. 用户提问
   ↓
2. 动态TopK检索
   - 文本库: 20-100个候选
   - 图片库: 5-20个候选
   ↓
3. 合并结果 (交替插入)
   ↓
4. Rerank重排序
   - 使用 bge-reranker-v2-m3
   - 返回top_k个最佳结果
   ↓
5. 检查context内容
   - 有图片? → 多模态模型
   - 纯文本? → 文本模型
   ↓
6. 生成回答
   - 如有图片引用，自动附加原图
   ↓
7. 返回结果
```

---

## 📊 性能提升

| 指标 | 重构前 | 重构后 | 提升 |
|-----|-------|-------|------|
| 文本检索候选数 | 3-10个 | 20-100个 | **3-10倍** |
| 检索精度 | 向量相似度 | 向量+Rerank | **显著提升** |
| 图片利用率 | 仅CLIP向量 | 向量+文本描述 | **双重检索** |
| 模型选择 | 固定 | 智能切换 | **成本优化** |
| 图片信息 | 分离 | 嵌入文本 | **统一检索** |

---

## 🔧 配置文件

### config.py 新增
```python
RERANK_MODEL_NAME = "Pro/BAAI/bge-reranker-v2-m3"
```

### 新增文件
- `image_describer.py` - 图片描述生成器
- `reranker.py` - Rerank重排序模块
- `向量库重构说明.md` - 本文档

### 修改文件
- `document_loader.py` - 集成图片描述生成
- `hybrid_retriever.py` - 动态TopK + Rerank
- `rag_agent.py` - 智能模型选择
- `config.py` - 添加Rerank模型配置

---

## 🚀 使用示例

### 重新处理数据
```bash
cd /home/honglianglu/hdd/rag-agent
rm -rf vector_db/nlp .mineru_cache
python process_data.py
```

### 测试Rerank
```bash
python reranker.py
```

### 测试图片描述
```bash
python image_describer.py
```

---

## ⚠️ 注意事项

1. **图片描述生成较慢**：每张图片需要调用多模态模型，建议并发处理
2. **Rerank API调用**：确保API支持rerank endpoint
3. **Token消耗**：图片描述和Rerank都会消耗token
4. **缓存机制**：MinerU已有缓存，避免重复处理
5. **图片路径**：确保图片路径正确，便于传给多模态模型

---

## 🎯 下一步优化建议

1. **缓存图片描述**：避免重复生成
2. **异步Rerank**：提高响应速度
3. **自适应TopK**：根据查询复杂度动态调整
4. **图片压缩**：减少传输大小
5. **批量Rerank**：一次处理多个查询

---

生成时间: 2025-12-19
版本: v2.0


