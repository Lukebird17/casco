# 新功能说明：知识库选择 & LaTeX公式渲染

## 🎉 新增功能

### 1. 对话框知识库选择器

**功能描述：**
- 在对话输入框上方添加了知识库选择下拉菜单
- 用户可以在提问前选择要检索的知识库
- 系统会自动切换到选定的知识库进行上下文检索

**使用方法：**
1. 在输入框上方找到数据库图标 🗄️ 和下拉菜单
2. 点击下拉菜单选择目标知识库
3. 输入问题并发送，系统将仅在选定的知识库中检索

**UI位置：**
```
┌─────────────────────────────────────────────┐
│  🗄️ [知识库选择] │ ⚡Fast  🧠Thinking    │
│                                             │
│  ┌───────────────────────────────────────┐ │
│  │ 向我提问任何问题...                    │ │
│  └───────────────────────────────────────┘ │
└─────────────────────────────────────────────┘
```

**后端实现：**
- 在chat请求中添加了`knowledge_base_id`参数
- 系统在检索前自动切换到指定的向量数据库
- 终端会打印当前使用的知识库信息

---

### 2. LaTeX公式渲染

**功能描述：**
- 支持在AI回复中渲染LaTeX数学公式
- 支持行内公式和块级公式
- 支持标准LaTeX和Markdown数学语法

**支持的语法：**

#### 行内公式（Inline Math）
```markdown
这是一个行内公式：$E = mc^2$
或者使用 \( E = mc^2 \)
```
渲染效果：这是一个行内公式：$E = mc^2$

#### 块级公式（Display Math）
```markdown
这是一个块级公式：

$$
\int_{-\infty}^{\infty} e^{-x^2} dx = \sqrt{\pi}
$$

或者使用：

\[
\frac{-b \pm \sqrt{b^2 - 4ac}}{2a}
\]
```

渲染效果：
$$
\int_{-\infty}^{\infty} e^{-x^2} dx = \sqrt{\pi}
$$

#### 常用数学符号示例

| LaTeX | 渲染效果 | 说明 |
|-------|---------|------|
| `$\alpha, \beta, \gamma$` | α, β, γ | 希腊字母 |
| `$\sum_{i=1}^{n} x_i$` | ∑ᵢ₌₁ⁿ xᵢ | 求和 |
| `$\int_a^b f(x)dx$` | ∫ₐᵇ f(x)dx | 积分 |
| `$\frac{a}{b}$` | a/b | 分数 |
| `$\sqrt{x}$` | √x | 平方根 |
| `$x^2, x_i$` | x², xᵢ | 上下标 |
| `$\lim_{x \to \infty}$` | lim(x→∞) | 极限 |

#### 矩阵示例
```latex
$$
\begin{bmatrix}
a & b \\
c & d
\end{bmatrix}
$$
```

---

## 🛠️ 安装依赖

LaTeX公式渲染需要额外的npm依赖包。

### 方法1：使用安装脚本（推荐）
```bash
cd /home/honglianglu/hdd/rag-agent/frontend
./install_latex_deps.sh
```

### 方法2：手动安装
```bash
cd /home/honglianglu/hdd/rag-agent/frontend

# 如果使用npm
npm install remark-math rehype-katex katex

# 如果使用yarn
yarn add remark-math rehype-katex katex

# 如果使用pnpm
pnpm add remark-math rehype-katex katex
```

### 方法3：使用conda环境中的node
```bash
conda activate rag
cd /home/honglianglu/hdd/rag-agent/frontend
npm install remark-math rehype-katex katex
```

---

## 📝 技术实现细节

### 知识库选择器
**前端组件：**
- `InputArea.jsx`：添加了知识库下拉菜单和状态管理
- `useChat.js`：在发送消息时传递`knowledgeBaseId`

**后端API：**
- `ChatRequest`：添加了`knowledge_base_id`字段
- `chat` endpoint：在检索前调用`vector_store.switch_collection()`切换知识库

### LaTeX渲染
**依赖库：**
- `remark-math`：解析Markdown中的数学公式
- `rehype-katex`：使用KaTeX渲染LaTeX公式
- `katex`：高性能的LaTeX渲染引擎

**配置：**
```jsx
<ReactMarkdown
  remarkPlugins={[remarkMath]}
  rehypePlugins={[rehypeKatex]}
>
  {message.content}
</ReactMarkdown>
```

---

## 🎯 使用场景

### 知识库选择器
1. **多学科知识管理**：不同课程使用不同知识库
2. **项目隔离**：不同项目的文档分开管理
3. **精准检索**：减少无关文档干扰

### LaTeX公式渲染
1. **数学问题**：显示数学公式和推导过程
2. **物理化学**：展示科学公式和方程
3. **工程计算**：显示复杂的计算公式
4. **论文写作**：预览LaTeX格式的公式

---

## 🐛 故障排除

### 问题1：知识库选择器显示为空
**原因：** 知识库列表加载失败

**解决方案：**
```bash
# 检查知识库目录
ls /home/honglianglu/hdd/rag-agent/vector_db/

# 确保至少有一个知识库
cd /home/honglianglu/hdd/rag-agent
python -c "from config import *; ensure_kb_dirs('default')"
```

### 问题2：公式不显示或显示为纯文本
**原因：** 缺少依赖或CSS样式未加载

**解决方案：**
1. 确认依赖已安装：
   ```bash
   cd frontend
   npm list remark-math rehype-katex katex
   ```

2. 检查浏览器控制台是否有CSS加载错误

3. 重新启动前端服务：
   ```bash
   cd frontend
   npm start
   ```

### 问题3：切换知识库后检索失败
**原因：** 向量数据库路径错误或数据库未初始化

**解决方案：**
```bash
# 检查向量数据库是否存在
ls /home/honglianglu/hdd/rag-agent/vector_db/<kb_id>/

# 重新初始化知识库
# 通过知识库管理面板上传文件
```

---

## 📊 性能优化

### 知识库切换优化
- 系统会缓存向量数据库连接
- 切换知识库时只重新加载collection
- 不会影响其他用户或会话

### 公式渲染优化
- KaTeX比MathJax快得多（服务器端渲染）
- 公式仅在消息显示时渲染一次
- 支持暗色模式下的公式显示

---

## 🔮 未来改进计划

1. **知识库组合检索**：同时检索多个知识库
2. **公式编辑器**：可视化编辑LaTeX公式
3. **公式搜索**：在文档中搜索特定公式
4. **知识库标签**：为知识库添加分类标签

---

## ✅ 测试清单

- [ ] 知识库选择器显示所有可用知识库
- [ ] 选择不同知识库后检索结果正确
- [ ] 行内公式正常渲染
- [ ] 块级公式正常渲染
- [ ] 希腊字母和特殊符号正常显示
- [ ] 矩阵和复杂公式正常显示
- [ ] 暗色模式下公式可读
- [ ] 后端终端打印正确的知识库信息

---

**更新日期：** 2025-12-19
**版本：** v2.3.0




