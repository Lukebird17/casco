# ✅ 修复知识图谱4字限制 - 简单操作指南

## 🎯 问题

知识图谱显示的中文实体仍然是4个字（如"隐马尔"、"马尔可"），而不是完整的术语（如"隐马尔可夫模型"）。

## ✅ 已完成

1. ✅ 代码已更新（2-4字 → 2-8字）
2. ✅ 旧缓存已清理（knowledge_graph.json已删除）

## 🔄 现在需要做的（3步）

### 步骤1：重启系统 ⏱️ 30秒

```bash
cd /home/honglianglu/hdd/rag-agent
./restart_all.sh
```

等待输出：
```
✅ 后端服务启动成功
✅ 前端服务启动成功
```

---

### 步骤2：触发重新生成 ⏱️ 1分钟

打开浏览器：`http://localhost:5173`

**方法A：重新上传一个文档（最简单）**
```
1. 点击"上传文档"
2. 选择任意一个PDF/文档
3. 上传
4. 等待处理完成
```

**方法B：使用现有文档重新生成**
```
1. 点击侧边栏"知识图谱"图标
2. 系统会自动从向量库重新提取
3. 等待加载完成
```

**重要提示**：
- 上传文档时，系统会用**新的8字规则**提取实体
- "隐马尔可夫模型"会被完整提取，不会被拆分

---

### 步骤3：验证结果 ⏱️ 10秒

```
1. 点击"知识图谱"
2. 点击"可视化"按钮
3. 查看节点名称
```

**✅ 应该看到**：
```
- 隐马尔可夫模型 (7个字！)
- 卷积神经网络 (6个字！)
- 自然语言处理 (6个字！)
- 反向传播算法 (5个字！)
```

**❌ 不应该看到**：
```
- 隐马尔 (4个字)
- 马尔可 (3个字)
- 可夫模 (3个字)
```

---

## 📊 原理说明

### 为什么需要重新上传/重新生成？

```
┌──────────────┐
│  PDF文档     │
└──────┬───────┘
       ↓ 
   [上传时提取]  ← 使用当时的规则
       ↓
┌──────────────┐
│  向量库数据   │  ← 已经分词和存储
└──────┬───────┘
       ↓
┌──────────────┐
│ knowledge_   │  ← 从向量库提取的
│ graph.json   │
└──────────────┘
```

**关键**：向量库中的数据是上传时就分词好的，所以：
- 旧数据：用4字规则分词 → "隐马尔"、"马尔可"
- 新数据：用8字规则分词 → "隐马尔可夫模型"

**解决**：重新上传文档，让系统用新规则处理。

---

## 🧪 快速验证

### 验证代码已更新

```bash
grep "{2,8}" /home/honglianglu/hdd/rag-agent/backend/api.py
grep "{2,8}" /home/honglianglu/hdd/rag-agent/knowledge_graph.py

# 应该看到两行输出，证明代码已更新
```

### 验证缓存已清理

```bash
ls /home/honglianglu/hdd/rag-agent/knowledge_graph.json

# 应该显示：No such file or directory
```

---

## 💡 补充说明

### 概念定位 vs 知识图谱

| 功能 | 数据来源 | 是否需要重新生成 |
|------|----------|------------------|
| 概念定位 | 实时从向量库提取 | ❌ 不需要（自动生效）|
| 知识图谱 | knowledge_graph.json | ✅ 需要重新生成 |

所以：
- **概念定位**可能已经显示完整术语（如果向量库是新数据）
- **知识图谱**需要重新生成才能显示完整术语

### 如何确认生成成功？

打开 `knowledge_graph.json` 文件，查找实体：

```bash
cat /home/honglianglu/hdd/rag-agent/knowledge_graph.json | grep -o '"[^"]*"' | head -20
```

应该看到完整的实体名称。

---

## 🎉 总结

1. **代码** ✅ 已更新（{2,8}）
2. **缓存** ✅ 已清理
3. **操作** ⏳ 重启 + 重新上传文档
4. **验证** ⏳ 查看可视化界面

**预计耗时**：2-3分钟

---

**日期**：2025-12-20  
**问题**：知识图谱仍显示4字实体  
**解决**：重新上传文档触发重新提取  
**状态**：✅ 准备就绪，等待操作

