# 多模态输入处理说明

## 🎯 处理逻辑

系统现在严格按照以下逻辑处理不同类型的输入：

### 1. 纯文本输入

**流程**：
```
用户问题 → 文字数据库检索 + 图片数据库检索 → 纯文本模型生成答案
```

**特点**：
- 同时在文字和图片向量库中检索相关内容
- 使用 `TEXT_MODEL_NAME` (Qwen/Qwen2.5-72B-Instruct)
- 最经济高效，适合纯文本问答

**示例**：
- "slub是什么？"
- "解释一下内存分配器"
- "比较buddy和slub的区别"

---

### 2. 图片输入

**流程**：
```
用户问题 + 图片 
  ↓
1. 多模态模型描述图片
  ↓
2. 将描述加入原问题，形成增强query
  ↓
3. 用增强query在向量库中检索
  ↓
4. 将原图片 + 检索到的context + 用户问题 → 多模态模型生成答案
```

**特点**：
- 先描述图片，增强检索效果
- 最终生成答案时使用**原始图片**（不是描述）
- 使用 `MULTIMODAL_MODEL_NAME` (Qwen/Qwen3-VL-32B-Instruct)
- 适合图表、公式、截图等视觉内容

**示例**：
- 上传架构图 + "解释这个系统的工作流程"
- 上传公式截图 + "这个公式的含义是什么？"
- 上传代码截图 + "这段代码有什么问题？"

---

### 3. 文件输入

**流程**：
```
用户问题 + 文件
  ↓
1. 读取文件内容（纯文本提取，不OCR）
  ↓
2. 用原始问题在向量库中检索
  ↓
3. 将文件内容 + 检索到的context + 用户问题 → 多模态模型生成答案
```

**特点**：
- 直接读取文件文本，**不进行OCR**
- 支持 PDF, DOCX, TXT, MD 等格式
- 使用 `MULTIMODAL_MODEL_NAME` (Qwen/Qwen3-VL-32B-Instruct)
- 适合上传论文、报告、代码文件等

**示例**：
- 上传PDF + "总结这篇文档的核心内容"
- 上传代码文件 + "找出这段代码的bug"
- 上传Word文档 + "提取关键观点"

---

## 🔧 配置说明

在 `config.py` 中配置两个模型：

```python
# 纯文本模型（用于纯文本问答）
TEXT_MODEL_NAME = "Qwen/Qwen2.5-72B-Instruct"

# 多模态模型（用于图片/文件输入）
MULTIMODAL_MODEL_NAME = "Qwen/Qwen3-VL-32B-Instruct"
```

**为什么需要两个模型？**
1. **性能优化**：纯文本问题不需要多模态能力，使用文本模型更快更经济
2. **效果优化**：多模态模型在处理视觉和复杂文档时效果更好
3. **成本控制**：根据实际需求选择合适的模型，节省API调用成本

---

## 📊 核心代码实现

### rag_agent.py - answer_question 方法

```python
def answer_question(self, query, image=None, file_content=None):
    if image:
        # 情况2: 有图片
        # 1. 描述图片
        result = handler.handle_image_input(image, query)
        enhanced_query = result['enhanced_query']
        
        # 2. 用增强query检索
        context, docs = self.retrieve_context(enhanced_query)
        
        # 3. 原图 + context → 多模态模型
        answer = self.generate_response(
            query, context,
            image=image,  # 原图！
            use_multimodal_model=True
        )
        
    elif file_content:
        # 情况3: 有文件
        # 1. 用原始query检索
        context, docs = self.retrieve_context(query)
        
        # 2. 文件内容 + context → 多模态模型
        answer = self.generate_response(
            query, context,
            file_content=file_content,
            use_multimodal_model=True
        )
        
    else:
        # 情况1: 纯文本
        # 1. 文字+图片库检索
        context, docs = self.retrieve_context(query)
        
        # 2. 纯文本模型
        answer = self.generate_response(
            query, context,
            use_multimodal_model=False
        )
    
    return answer
```

### multimodal_input_handler.py - 图片处理

```python
def handle_image_input(self, image, text_query):
    # 1. 保存图片
    temp_image_path = self._save_temp_image(image)
    
    # 2. 用多模态模型描述图片
    description = self._describe_image(temp_image_path)
    
    # 3. 增强查询
    enhanced_query = f"{text_query}\n\n【用户上传的图片描述】\n{description}"
    
    return {
        "enhanced_query": enhanced_query,
        "image_path": temp_image_path,
        "image_description": description
    }
```

---

## 🚀 使用示例

### 前端 - 发送消息

```javascript
// 纯文本
await sendMessage({
  message: "什么是slub？",
  session_id: sessionId,
});

// 图片
await sendMessage({
  message: "解释这张架构图",
  session_id: sessionId,
  image_base64: "data:image/png;base64,iVBORw0KG...",
});

// 文件
await sendMessage({
  message: "总结这个文档",
  session_id: sessionId,
  file_content: "文档的完整文本内容...",
});
```

### 后端 - API调用

```python
POST /api/chat
Content-Type: application/json

{
  "message": "用户问题",
  "session_id": "session_xxx",
  "enable_socratic": false,
  "image_base64": "data:image/png;base64,...",  // 可选
  "file_content": "文件内容..."                // 可选
}
```

---

## ✅ 验证检查清单

启动后端后，查看日志确认：

1. **纯文本输入**：
   ```
   📝 纯文本输入，使用文本模型
   🌐 正在调用LLM API (Qwen/Qwen2.5-72B-Instruct - 文本模型)...
   ```

2. **图片输入**：
   ```
   📷 检测到图片输入，使用多模态处理流程
   📷 已保存图片: /tmp/...
   🔍 使用多模态模型描述图片...
   ✅ 图片描述: ...
   🌐 正在调用LLM API (Qwen/Qwen3-VL-32B-Instruct - 多模态模型)...
   🖼️ 包含图片输入
   ```

3. **文件输入**：
   ```
   📄 检测到文件输入，使用多模态处理流程
   📄 读取文件: ...
   ✅ 读取成功: 1234 字符
   🌐 正在调用LLM API (Qwen/Qwen3-VL-32B-Instruct - 多模态模型)...
   📄 包含文件内容 (1234 字符)
   ```

---

## 🎓 设计理念

### 为什么这样设计？

1. **检索效率最大化**
   - 图片：先描述再检索，确保检索相关性
   - 文件：用原始问题检索，避免文件内容干扰检索

2. **信息完整性**
   - 图片：传原图给模型，而不是描述，保证视觉信息不丢失
   - 文件：直接传文本，无需OCR，速度快且准确

3. **成本效益平衡**
   - 纯文本用便宜的文本模型
   - 多模态场景才用昂贵的多模态模型
   - 避免过度使用多模态能力

---

## 📝 修改记录

**2025-12-18**
- ✅ 重构 `rag_agent.py` 的 `answer_question` 方法
- ✅ 确保图片输入时传递原始图片到多模态模型
- ✅ 确保文件输入不走OCR，直接读取文本
- ✅ 纯文本输入强制使用文本模型
- ✅ 图片和文件输入强制使用多模态模型
- ✅ 更新前后端API接口，支持 `file_content` 参数

---

**配置文件**: `config.py`  
**核心代码**: `rag_agent.py`, `multimodal_input_handler.py`  
**API文档**: `backend/api.py`




