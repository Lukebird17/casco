# 🧪 LLM增强 - 快速测试指南

## 🎯 测试目标

验证两个LLM增强功能：
1. **概念筛选**：LLM智能过滤无意义词
2. **知识图谱**：LLM深度理解实体和关系

---

## 🚀 测试步骤

### 步骤1：重启系统

```bash
cd /home/honglianglu/hdd/rag-agent
./restart_all.sh
```

**等待输出**：
```
✅ 后端服务启动成功
✅ 前端服务启动成功
```

---

### 步骤2：测试概念筛选（Web UI）

#### 2.1 打开浏览器

```
http://localhost:5173
```

#### 2.2 上传测试文档

- 点击"上传文档"
- 选择一个包含技术内容的PDF（如机器学习、深度学习相关）

#### 2.3 打开概念定位

- 点击侧边栏"概念定位"图标（⚡）
- 等待加载（约1-3秒）

#### 2.4 验证结果

**✅ 应该看到**：
```
热门概念：
1. 深度学习
2. 神经网络
3. Transformer
4. 卷积神经网络
5. 自然语言处理
...
```

**❌ 不应该看到**：
```
- colspan, rowspan
- jpg, png, images
- mathbb, frac
- table, figure
- 任何HTML/LaTeX标记
```

#### 2.5 查看后端日志

在终端中应该看到：
```bash
🤖 使用LLM智能筛选概念（候选：100个）...
✅ LLM筛选: 100 → 30 个概念
```

---

### 步骤3：测试知识图谱（命令行）

#### 3.1 运行测试脚本

```bash
cd /home/honglianglu/hdd/rag-agent
python knowledge_graph_improved.py
```

#### 3.2 观察输出

**应该看到**：

```
============================================================
🧪 测试改进的知识图谱系统
============================================================

============================================================
📊 方法1: 统计方法（jieba TextRank + TF-IDF）
============================================================

提取的实体（15个）：
  ✅ 深度学习 (概念) - 重要度: 0.856
  ✅ 神经网络 (概念) - 重要度: 0.742
  ✅ 卷积神经网络 (概念) - 重要度: 0.698
  ...

提取的关系（10条）：
  ✅ 深度学习 --[相关]--> 神经网络
  ✅ 卷积神经网络 --[相关]--> 图像数据
  ...

============================================================
🤖 方法2: LLM增强（统计 + LLM优化）
============================================================
🤖 使用LLM增强知识图谱提取...
✅ LLM提取: 12个实体, 15条关系
📊 实体合并: LLM(12) + 统计(15) → 18
📊 关系合并: LLM(15) + 统计(10) → 20

提取的实体（18个）：
  ✅ 深度学习 (概念) - 机器学习的一个分支，使用神经网络
  ✅ 卷积神经网络 (技术) - 特别适合处理图像数据，能够自动提取特征
  ✅ ResNet (术语) - 著名的CNN架构
  ✅ VGG (术语) - 著名的CNN架构
  ✅ Transformer (技术) - 革新了自然语言处理领域
  ✅ BERT (术语) - 基于Transformer的预训练模型
  ✅ GPT (术语) - 基于Transformer的预训练模型
  ✅ Google (组织) - BERT的开发者
  ✅ OpenAI (组织) - GPT的开发者
  ...

提取的关系（20条）：
  ✅ 深度学习 --[包含]--> 卷积神经网络
      💡 卷积神经网络是深度学习的一个分支
  ✅ 卷积神经网络 --[用于]--> 图像数据
      💡 CNN特别适合处理图像数据
  ✅ ResNet --[属于]--> 卷积神经网络
      💡 ResNet是著名的CNN架构
  ✅ Transformer --[应用于]--> 自然语言处理
      💡 Transformer革新了NLP领域
  ✅ BERT --[基于]--> Transformer
      💡 BERT使用Transformer架构
  ✅ GPT --[基于]--> Transformer
      💡 GPT使用Transformer架构
  ✅ Google --[开发]--> BERT
      💡 Google开发了BERT模型
  ✅ OpenAI --[开发]--> GPT
      💡 OpenAI开发了GPT模型
  ...

============================================================
📈 对比总结
============================================================
统计方法: 15个实体, 10条关系
LLM增强:  18个实体, 20条关系
============================================================
```

---

## ✅ 验收标准

### 概念筛选

- [ ] 不出现HTML标签（colspan、rowspan、span、div等）
- [ ] 不出现图片格式（jpg、png、images等）
- [ ] 不出现LaTeX命令（mathbb、frac、sqrt等）
- [ ] 只显示有意义的专业术语
- [ ] 概念与文档内容相关
- [ ] 后端日志显示"🤖 使用LLM智能筛选"

### 知识图谱

#### 实体质量

- [ ] 实体是完整的词组（如"深度学习"而不是"学习"）
- [ ] 实体有正确的类型标注（人物、地点、组织、概念、技术、术语、动作）
- [ ] 实体有描述信息
- [ ] 不出现无意义的词

#### 关系质量

- [ ] 关系类型不只是"相关"
- [ ] 关系类型有语义含义（包含、属于、使用、实现、基于等）
- [ ] 关系有描述信息（💡）
- [ ] source和target都是有效的实体

#### 数量对比

- [ ] LLM增强提取的实体数 ≥ 统计方法
- [ ] LLM增强提取的关系数 ≥ 统计方法
- [ ] 后端日志显示"🤖 使用LLM增强知识图谱提取"

---

## 🐛 问题排查

### 问题1：概念筛选没有调用LLM

**症状**：
- 后端日志没有"🤖 使用LLM智能筛选"
- 仍然出现无意义词

**排查**：
```bash
# 1. 检查开关是否启用
grep -n "use_llm_filter = " /home/honglianglu/hdd/rag-agent/backend/api.py

# 应该显示：
# use_llm_filter = True

# 2. 检查错误日志
tail -n 50 /home/honglianglu/hdd/rag-agent/backend/api_*.log
```

**解决**：
```bash
# 如果开关是False，修改为True
# 然后重启
./restart_all.sh
```

---

### 问题2：知识图谱没有调用LLM

**症状**：
- 测试脚本没有显示"🤖 使用LLM增强"
- 关系只有"相关"类型

**排查**：
```bash
# 1. 检查测试脚本
grep -n "use_llm=" /home/honglianglu/hdd/rag-agent/knowledge_graph_improved.py

# 应该显示：
# use_llm=True

# 2. 运行并查看错误
python knowledge_graph_improved.py 2>&1 | tee test_output.txt
```

**解决**：
```python
# 在测试代码中确保
entities, rels = kg.extract_entities_and_relations(
    test_text,
    "测试文档",
    use_llm=True  # ← 确保为True
)
```

---

### 问题3：LLM调用失败

**症状**：
- 后端日志显示"⚠️  LLM筛选失败"或"⚠️  LLM提取失败"
- 降级到统计方法

**常见原因**：
1. API Key无效或额度不足
2. 网络连接问题
3. 模型响应超时

**排查**：
```bash
# 1. 检查API配置
cat /home/honglianglu/hdd/rag-agent/.env | grep OPENAI

# 2. 测试API连接
python -c "
from openai import OpenAI
from config import OPENAI_API_KEY, OPENAI_API_BASE, MODEL_NAME
client = OpenAI(api_key=OPENAI_API_KEY, base_url=OPENAI_API_BASE)
response = client.chat.completions.create(
    model=MODEL_NAME,
    messages=[{'role': 'user', 'content': '你好'}],
    max_tokens=10
)
print('✅ API连接正常')
print(response.choices[0].message.content)
"
```

**解决**：
```bash
# 检查余额、网络等
# 如果API不可用，系统会自动降级到统计方法
```

---

### 问题4：LLM返回格式错误

**症状**：
- 后端日志显示JSON解析错误
- LLM筛选失败

**排查**：
```bash
# 查看详细错误日志
tail -n 100 /home/honglianglu/hdd/rag-agent/backend/api_*.log | grep -A 10 "LLM"
```

**解决**：
- 这个问题通常是暂时的（LLM偶尔会返回格式不标准的结果）
- 系统有容错机制，会自动降级
- 如果频繁出现，可以调整temperature参数（降低到0.1）

---

## 📊 性能基准

### 正常表现

| 指标 | 预期值 | 说明 |
|------|--------|------|
| 概念筛选LLM调用时间 | 1-3秒 | 取决于候选数量 |
| 概念筛选准确率 | >90% | 无意义词<10% |
| 知识图谱LLM调用时间 | 3-5秒 | 取决于文本长度 |
| 知识图谱实体完整性 | >85% | 实体为完整词组 |
| 知识图谱关系多样性 | >70% | 非"相关"关系占比 |

### 异常情况

| 现象 | 可能原因 | 处理 |
|------|----------|------|
| 调用时间>10秒 | 网络慢/文本太长 | 检查网络/减少文本长度 |
| 准确率<50% | LLM未启用 | 检查配置开关 |
| 频繁失败 | API额度不足 | 检查余额 |

---

## 🎯 效果示例

### 好的结果

**概念筛选**：
```
✅ 深度学习 (128次)
✅ 神经网络 (98次)
✅ Transformer (85次)
✅ 卷积神经网络 (72次)
✅ 自然语言处理 (68次)
✅ BERT (58次)
✅ GPT (52次)
✅ 注意力机制 (48次)
✅ ResNet (42次)
✅ 图像识别 (38次)
```

**知识图谱**：
```
实体：
  深度学习 (概念) - 机器学习的一个分支
  BERT (术语) - 基于Transformer的预训练模型
  Google (组织) - BERT的开发者

关系：
  深度学习 --[包含]--> 卷积神经网络
  BERT --[基于]--> Transformer
  Google --[开发]--> BERT
```

---

### 不好的结果（需要排查）

**概念筛选**：
```
❌ colspan (189次)
❌ rowspan (156次)
❌ jpg (142次)
⚠️  学习 (128次)  # 不完整
⚠️  网络 (98次)   # 不完整
```

**知识图谱**：
```
实体：
  ❌ 学习 (概念)  # 不完整
  ❌ 网络 (概念)  # 不完整

关系：
  ❌ 学习 --[相关]--> 网络  # 只有"相关"
  ❌ 数据 --[相关]--> 模型  # 只有"相关"
```

---

## 🔄 回滚方案

如果LLM功能出现问题，可以快速禁用：

### 禁用概念筛选LLM

```bash
# 编辑 backend/api.py
nano /home/honglianglu/hdd/rag-agent/backend/api.py

# 找到这一行（约1682行）：
use_llm_filter = True

# 改为：
use_llm_filter = False

# 保存并重启
./restart_all.sh
```

### 禁用知识图谱LLM

```python
# 在调用时设置use_llm=False
entities, rels = kg.extract_entities_and_relations(
    text,
    source="文档",
    use_llm=False  # ← 禁用LLM
)
```

---

## 📝 测试检查清单

### 准备阶段
- [ ] 后端和前端服务已启动
- [ ] API配置正确
- [ ] 有测试文档可用

### 概念筛选测试
- [ ] 打开概念定位功能
- [ ] 观察到无意义词被过滤
- [ ] 后端日志显示LLM调用
- [ ] 处理时间在1-3秒内

### 知识图谱测试
- [ ] 运行测试脚本
- [ ] 看到统计方法和LLM方法对比
- [ ] 实体完整且有类型
- [ ] 关系类型多样化
- [ ] 有描述信息

### 性能测试
- [ ] 响应时间可接受
- [ ] 没有频繁失败
- [ ] 结果质量符合预期

---

## 🎉 成功标准

当以下所有条件满足时，表示测试成功：

1. ✅ 概念筛选不出现无意义词
2. ✅ 概念筛选后端日志显示LLM调用
3. ✅ 知识图谱实体完整且准确
4. ✅ 知识图谱关系类型多样化
5. ✅ 知识图谱有描述信息
6. ✅ 处理时间在可接受范围内
7. ✅ 没有系统错误

---

**测试时间**：预计10-15分钟  
**准备**：无需额外安装，已集成到系统中  
**反馈**：测试完成后，请报告结果！ 🚀

