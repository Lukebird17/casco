# 文档查看功能完整说明

## 功能概述

文档查看器支持**实时在线查看**以下文件类型：
- ✅ **PDF** - 显示原始PDF文件（保留格式、图片、表格）
- ✅ **TXT/MD** - 显示纯文本（支持滚动、复制）
- ✅ **图片** - JPG、PNG、GIF、WEBP、BMP、SVG
- ✅ **DOCX** - 转换为HTML实时预览（保留基本格式）
- ✅ **PPTX** - 显示幻灯片文本内容

---

## 安装依赖

在使用前，请先安装必要的依赖：

```bash
cd /home/honglianglu/hdd/rag-agent
bash 安装文档查看依赖.sh
```

**核心依赖：**
- `mammoth` - 用于DOCX转HTML
- `PyPDF2` - 用于PDF处理
- `python-docx` - 用于DOCX处理
- `python-pptx` - 用于PPTX处理

---

## 使用方法

### 方式1：从对话引用打开

1. 在对话中收到AI回复
2. 点击消息下方的 **"引用文档"**
3. 点击 **"查看文档"** 按钮
4. 文档查看器自动打开并显示对应页面

### 方式2：从工具面板打开

1. 点击侧边栏 **"文档查看"** 图标
2. 从左侧文档列表选择文件
3. 右侧自动显示文档内容

### 方式3：从文档大纲跳转

1. 打开 **"文档大纲"** 面板
2. 选择文档并点击章节
3. 自动打开文档查看器并跳转到对应位置

### 方式4：从概念定位跳转

1. 打开 **"概念定位"** 面板
2. 搜索关键词
3. 点击搜索结果，自动打开文档并定位

---

## 查看器功能详解

### 1. PDF 查看

**特性：**
- ✅ 显示原始PDF（100%还原）
- ✅ 支持页码导航
- ✅ 缩放功能（50%-200%）
- ✅ 复制文本
- ✅ 跳转到指定页面

**控制栏：**
```
[上一页] 第 X 页 [下一页]    [缩小] 100% [放大]
```

**示例代码（后端）：**
```python
@app.get("/api/documents/{filename}/raw")
async def get_document_raw(filename: str):
    return FileResponse(filepath, media_type='application/pdf')
```

**示例代码（前端）：**
```jsx
<iframe
  src={`http://localhost:8000/api/documents/${filename}/raw#page=${page}`}
  className="w-full h-full"
/>
```

---

### 2. TXT/MD 文本查看

**特性：**
- ✅ 原始文本显示
- ✅ 保留换行和缩进
- ✅ 支持中英文
- ✅ 复制任意文本
- ✅ 滚动查看

**查看器样式：**
- 等宽字体（适合代码）
- 白色背景
- 自动换行

**后端实现：**
```python
return FileResponse(filepath, media_type='text/plain; charset=utf-8')
```

---

### 3. 图片查看

**支持格式：**
- JPG/JPEG
- PNG
- GIF（动图）
- WEBP
- BMP
- SVG

**特性：**
- ✅ 原图显示
- ✅ 缩放功能（10%-500%）
- ✅ 适应屏幕
- ✅ 深色背景（护眼）
- ✅ 右键保存

**控制栏：**
```
[缩小] 100% [放大] [重置]
```

---

### 4. DOCX 查看（转HTML）

**特性：**
- ✅ 保留基本格式（标题、段落、列表）
- ✅ 保留文本样式（粗体、斜体）
- ✅ 保留表格结构
- ✅ 可复制文本
- ✅ 滚动查看

**转换技术：**
使用 `mammoth` 库将DOCX转为HTML：

```python
import mammoth

with open(filepath, 'rb') as docx_file:
    result = mammoth.convert_to_html(docx_file)
    html_content = result.value
```

**样式优化：**
- 最大宽度：900px
- 清晰的排版
- 美观的字体

**注意事项：**
- ⚠️ 复杂的样式可能丢失
- ⚠️ 嵌入的图片需要特殊处理
- ⚠️ 建议原始文档使用简单格式

---

### 5. PPTX 查看

**特性：**
- ✅ 显示幻灯片文本内容
- ✅ 页码导航
- ✅ 清晰的排版

**限制：**
- ⚠️ 仅显示文本内容（不包含图片、动画）
- ⚠️ 完整的图文预览需要LibreOffice

**控制栏：**
```
[上一页] 第 X 页 / 共 Y 页 [下一页]
```

**改进方案（未来）：**
1. 使用LibreOffice将PPTX转PDF
2. 使用pdf2image将PDF转图片
3. 显示幻灯片图片序列

---

## 后端API接口

### 获取原始文件

```http
GET /api/documents/{filename}/raw

支持类型：PDF, TXT, MD, 图片

返回：文件原始内容
Content-Type: 根据文件类型自动设置
```

### DOCX预览（转HTML）

```http
GET /api/documents/{filename}/preview/docx

返回：HTML格式的文档内容
Content-Type: text/html
```

### PPTX预览（文本）

```http
GET /api/documents/{filename}/preview/pptx?page=1

参数：
  - page: 页码（从1开始）

返回：HTML格式的幻灯片内容
```

---

## 文件类型检测

前端自动检测文件类型并选择合适的查看器：

```javascript
const getFileType = (filename) => {
  const ext = filename.toLowerCase().split('.').pop();
  
  if (ext === 'pdf') return 'pdf';
  if (['txt', 'md'].includes(ext)) return 'text';
  if (['jpg', 'jpeg', 'png', 'gif', 'webp', 'bmp', 'svg'].includes(ext)) 
    return 'image';
  if (ext === 'docx') return 'docx';
  if (ext === 'pptx') return 'pptx';
  
  return 'unknown';
};
```

---

## 查看器UI组件

### 布局结构

```
┌─────────────────────────────────────────────┐
│  ┌──────────┐  ┌──────────────────────────┐ │
│  │ 文档列表  │  │   头部栏                 │ │
│  │          │  │  [文档名] [下载] [关闭]   │ │
│  │ □ doc1   │  ├──────────────────────────┤ │
│  │ ■ doc2   │  │                          │ │
│  │ □ doc3   │  │   文档内容显示区域         │ │
│  │ □ doc4   │  │                          │ │
│  │          │  │                          │ │
│  │          │  ├──────────────────────────┤ │
│  │          │  │   控制栏（可选）          │ │
│  │          │  │  [页码] [缩放]           │ │
│  └──────────┘  └──────────────────────────┘ │
└─────────────────────────────────────────────┘
```

### 左侧文档列表

- 显示所有可用文档
- 文件类型图标（PDF、图片、文本等）
- 文件大小
- 选中高亮
- 点击切换文档

### 右侧查看区域

根据文件类型动态渲染：
- **PDF/图片** - iframe或img
- **文本** - pre或textarea
- **DOCX** - iframe加载HTML
- **PPTX** - 自定义HTML布局

---

## 交互功能

### 复制文本

✅ **支持的格式：**
- TXT/MD - 直接复制
- PDF - 浏览器原生支持
- DOCX - HTML内容可复制

❌ **不支持的格式：**
- 图片中的文字（需OCR）

### 滚动

所有格式都支持滚动查看：
- 鼠标滚轮
- 触控板手势
- 滚动条拖动

### 缩放

✅ **支持缩放的格式：**
- PDF - 50%-200%
- 图片 - 10%-500%

✅ **操作方式：**
- 点击 [+] [-] 按钮
- 显示当前缩放比例

### 页面导航

✅ **支持的格式：**
- PDF - 页码跳转
- PPTX - 幻灯片切换

✅ **操作方式：**
- [上一页] [下一页] 按钮
- 显示当前页 / 总页数

### 下载

所有文件都支持下载：
- 点击头部栏的 [下载] 按钮
- 使用原始文件名

---

## 文件上传与查看流程

### 1. 用户上传文件

```
用户上传 → 保存到data目录 → 处理成向量 → 存入知识库
```

### 2. 文档列表加载

```
GET /api/documents
→ 返回data目录中所有文件
→ 前端显示文件列表
```

### 3. 查看文档

```
用户点击文档 → 检测文件类型 → 调用对应API → 渲染查看器
```

### 4. 引用跳转

```
AI回复 → 包含引用 → 用户点击查看 → 自动打开文档 → 跳转到页面/位置
```

---

## 性能优化

### 1. 延迟加载

- 文档列表：仅加载文件元数据
- 文档内容：选中时才加载
- 图片：懒加载

### 2. 缓存

- 浏览器缓存已查看的文档
- iframe自动缓存

### 3. 大文件处理

**PDF：**
- 浏览器原生支持流式加载
- 可以先显示前几页

**文本：**
- 限制最大显示长度
- 超过100万字符截断

**图片：**
- 自动压缩大尺寸图片
- 显示加载进度

---

## 已知限制

### 1. PPTX

- ❌ 无法显示图片、图表
- ❌ 无法显示动画效果
- ✅ 可显示文本内容
- 💡 建议：将PPTX转为PDF再上传

### 2. 加密文档

- ❌ 不支持密码保护的PDF
- ❌ 不支持加密的DOCX
- 💡 建议：先解密再上传

### 3. 扫描版PDF

- ❌ 无法提取文本
- ❌ 无法搜索
- ✅ 可以显示图片
- 💡 建议：使用OCR工具预处理

### 4. 大文件

- ⚠️ 超过50MB的文件加载慢
- ⚠️ 可能导致浏览器卡顿
- 💡 建议：分割大文件

---

## 故障排查

### 问题1：DOCX显示"转换功能未启用"

**原因：** mammoth库未安装

**解决：**
```bash
conda activate rag
pip install mammoth
```

### 问题2：PDF无法显示

**原因：** 
- 文件路径错误
- CORS问题
- 浏览器不支持

**解决：**
```bash
# 1. 检查文件是否存在
ls /home/honglianglu/hdd/rag-agent/data/

# 2. 检查后端日志
tail -f backend/nohup.out

# 3. 使用Chrome浏览器
```

### 问题3：图片不显示

**原因：**
- 文件格式不支持
- 文件损坏

**解决：**
```bash
# 验证图片文件
file /home/honglianglu/hdd/rag-agent/data/image.jpg

# 转换为标准格式
convert image.jpg -quality 90 image_fixed.jpg
```

### 问题4：文本显示乱码

**原因：** 编码问题

**解决：**
```bash
# 转换为UTF-8
iconv -f gbk -t utf-8 file.txt > file_utf8.txt
```

---

## 最佳实践

### 1. 文件准备

✅ **推荐格式：**
- 文档：PDF（最佳兼容性）
- 图片：PNG/JPG（标准格式）
- 文本：UTF-8编码的TXT

✅ **文件规范：**
- 文件名使用英文或数字
- 避免特殊字符
- 大小控制在50MB以内

### 2. 文档组织

建议按主题分类：
```
data/
  ├── 操作系统/
  │   ├── OS-01.pdf
  │   ├── OS-02.pdf
  │   └── notes.txt
  ├── 数据库/
  │   ├── mysql.pdf
  │   └── redis.md
  └── 编程/
      ├── python.pdf
      └── examples.py
```

### 3. 上传流程

1. **知识库管理** - 创建专门的知识库
2. **上传文件** - 批量上传相关文档
3. **验证** - 在文档列表中查看
4. **测试** - 打开查看器测试显示效果

---

## API完整清单

### 文档查看相关API

```
GET  /api/documents                    # 获取文档列表
GET  /api/documents/{name}             # 获取文档内容（文本）
GET  /api/documents/{name}/raw         # 获取原始文件
GET  /api/documents/{name}/outline     # 获取文档大纲
GET  /api/documents/{name}/search      # 搜索文档内容
GET  /api/documents/{name}/preview/docx   # DOCX预览
GET  /api/documents/{name}/preview/pptx   # PPTX预览
POST /api/knowledge-bases/{id}/upload  # 上传文件
```

---

## 键盘快捷键（计划中）

```
Ctrl/Cmd + F     - 在文档中搜索
Ctrl/Cmd + +     - 放大
Ctrl/Cmd + -     - 缩小
Ctrl/Cmd + 0     - 重置缩放
PageUp/PageDown  - 翻页（PDF/PPTX）
Home/End         - 跳转到开头/结尾
Esc              - 关闭查看器
```

---

## 更新日志

**v2.0 (2025-12-18)**
- ✅ 新增：DOCX转HTML实时预览
- ✅ 新增：PPTX文本预览
- ✅ 新增：图片缩放功能
- ✅ 新增：PDF页码导航
- ✅ 改进：文档列表UI
- ✅ 修复：文件下载功能

**v1.0 (初始版本)**
- ✅ 支持PDF、TXT、图片查看
- ✅ 基本的文档列表
- ✅ 简单的查看器

---

**🎉 现在您可以在系统中流畅地查看各种文档了！**



