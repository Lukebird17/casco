# 🚀 启动说明

由于需要在 `rag` conda 环境中运行，建议使用以下方式启动：

## 📋 方式 1：手动启动（推荐）

### 步骤 1：启动后端

打开**第一个终端**：

```bash
cd /home/honglianglu/hdd/rag-agent
conda activate rag
./start_backend.sh
```

或者直接：

```bash
cd /home/honglianglu/hdd/rag-agent
conda activate rag
cd backend
python api.py
```

### 步骤 2：启动前端

打开**第二个终端**：

```bash
cd /home/honglianglu/hdd/rag-agent
conda activate rag
./start_frontend.sh
```

或者直接：

```bash
cd /home/honglianglu/hdd/rag-agent/frontend
conda activate rag
npm run dev
```

## 📍 访问地址

启动成功后，访问：

- 🌐 **前端界面：** http://localhost:5173
- 📡 **后端 API：** http://localhost:8000
- 📖 **API 文档：** http://localhost:8000/docs

## 🛑 停止服务

在各自的终端按 `Ctrl+C` 停止服务。

---

## 🔧 首次运行

首次运行前端时，会自动安装依赖（约 1-2 分钟）。

---

## ❓ 常见问题

### Q: npm 命令找不到？
A: 确保已激活 rag 环境：`conda activate rag`

### Q: 后端报错 chromadb 找不到？
A: 确保在 rag 环境中运行后端

### Q: 前端无法连接后端？
A: 检查后端是否正常启动（http://localhost:8000）





