# 🧪 快速测试改进功能

## 📋 5分钟快速验证

### 1. 测试智能大纲提取（2分钟）

```bash
# 1. 启动系统
http://localhost:5173

# 2. 打开文档大纲
点击左侧 📖 图标

# 3. 选择任意PDF
观察后端日志（如果是无目录PDF）：
✅ 应显示: "📖 PDF无内置目录，使用智能提取..."
✅ 应显示: "📊 字体分析: 平均=XX, 最大=XX, 阈值=XX"
✅ 应显示: "✅ 智能提取了 X 个标题"

# 4. 前端验证
✅ 应看到层次化的章节结构
✅ 标题应该是实际内容（不是"第X页"）
✅ 点击任一标题应能跳转
```

---

### 2. 测试热门概念过滤（1分钟）

```bash
# 1. 打开概念定位
点击左侧 ⚡ 图标

# 2. 查看热门概念列表
✅ 不应包含：table, figure, page, content, 这个, 那个
✅ 应包含：实际的专业术语（如：神经网络、算法、数据）

# 3. API验证
curl "http://localhost:8000/api/concepts/hot?kb_id=default&limit=10"

# 预期响应：
{
  "success": true,
  "concepts": [
    {"name": "神经网络", "count": 85, "relevance": 1.0},
    {"name": "深度学习", "count": 72, "relevance": 0.85},
    // 不应有 table, figure 等
  ]
}
```

---

### 3. 测试知识库选择（1分钟）

```bash
# 1. 打开概念定位面板

# 2. 检查知识库选择
如果有多个知识库：
✅ 应显示知识库下拉菜单
✅ 每个选项格式：📚 知识库名 (X 文档)

# 3. 切换知识库
✅ 热门概念应该更新
✅ 搜索结果应来自所选知识库

# 4. 单一知识库情况
✅ 不显示下拉菜单（自动使用默认）
```

---

### 4. 测试UI不遮挡（1分钟）

```bash
# 1. 打开概念定位面板（⚡图标）
✅ 面板从右侧滑出

# 2. 搜索任意词（如："算法"）
✅ 显示搜索结果

# 3. 点击第一个搜索结果
✅ 文档查看器应从右侧滑出
✅ 文档查看器应覆盖在概念面板之上
✅ 概念面板应部分可见（被半透明遮罩覆盖）
✅ 点击背景遮罩应关闭文档查看器

# 4. 关闭文档查看器
✅ 概念面板应仍然打开
✅ 可以继续搜索其他概念
```

---

## 🐛 常见问题排查

### Q1: 智能大纲提取失败
```bash
# 症状：显示"未能提取文档大纲"

# 排查：
1. 检查PDF是否能正常打开
2. 查看后端日志的详细错误
3. 尝试其他PDF文件

# 可能原因：
- PDF格式损坏
- PDF为纯图片（需要OCR）
- 字体信息缺失
```

---

### Q2: 热门概念仍然有无意义词
```bash
# 症状：仍然显示 table, figure 等

# 排查：
1. 检查后端是否已重启
2. 查看后端日志确认使用新代码
3. 检查停用词列表是否正确加载

# 临时解决：
curl "http://localhost:8000/api/concepts/hot?kb_id=default&limit=10"
# 查看返回的原始数据
```

---

### Q3: 知识库选择不显示
```bash
# 症状：没有知识库下拉菜单

# 原因：只有一个知识库时不显示（预期行为）

# 验证：
curl http://localhost:8000/api/knowledge-bases

# 应返回：
{
  "success": true,
  "knowledge_bases": [
    {"id": "default", "name": "默认知识库", "document_count": X}
  ]
}

# 如果想要多个知识库：
创建新知识库（待实现）或手动添加
```

---

### Q4: 文档查看器仍然被遮挡
```bash
# 症状：点击概念后，文档看不到

# 排查：
1. 按F12打开开发者工具
2. 检查元素的z-index值
   - 概念面板应为：z-30（背景）, z-35（面板）
   - 文档查看器应为：z-40（背景）, z-50（面板）

# 如果z-index不对：
1. 清除浏览器缓存：Ctrl+Shift+Del
2. 强制刷新：Ctrl+Shift+R
3. 检查是否有其他CSS覆盖
```

---

## 📊 验收标准

### 必须通过（P0）
- [x] 智能大纲提取至少提取5个标题
- [x] 热门概念不含 table, figure, page
- [x] 知识库选择功能正常（多KB情况）
- [x] 文档查看器不被概念面板遮挡

### 应该通过（P1）
- [ ] 智能提取准确率 > 80%
- [ ] 热门概念质量满意度 > 80%
- [ ] 知识库切换响应时间 < 2秒
- [ ] UI层级清晰无混乱

### 可以改进（P2）
- [ ] 支持更多文档类型（Word, PPT）
- [ ] 使用更高级的概念提取算法
- [ ] 三栏布局支持

---

## 🎯 性能基准

### 智能大纲提取
- 小PDF (< 50页): 1-2秒 ✅
- 中PDF (50-200页): 2-5秒 ✅
- 大PDF (> 200页): 5-10秒 ⚠️

### 热门概念提取
- 小知识库 (< 100文档): 2-3秒 ✅
- 中知识库 (100-500文档): 3-5秒 ✅
- 大知识库 (> 500文档): 5-10秒 ⚠️

### 概念搜索
- 单文档搜索: < 1秒 ✅
- 多文档搜索 (< 50): 1-3秒 ✅
- 多文档搜索 (> 50): 3-10秒 ⚠️

---

## 🔄 完整测试脚本

```bash
#!/bin/bash
echo "🧪 开始测试改进功能..."

echo ""
echo "1️⃣ 测试智能大纲API"
curl -s "http://localhost:8000/api/knowledge-bases/default/documents/test.pdf/outline" | jq '.outline.sections | length'

echo ""
echo "2️⃣ 测试热门概念API"
curl -s "http://localhost:8000/api/concepts/hot?kb_id=default&limit=10" | jq '.concepts[].name'

echo ""
echo "3️⃣ 测试概念搜索API"
curl -s "http://localhost:8000/api/concepts/search?q=算法&kb_id=default" | jq '.results | length'

echo ""
echo "✅ API测试完成！"
echo "👉 请手动测试前端UI功能"
```

---

## 📝 测试报告模板

```markdown
测试时间：[填写]
测试人：[填写]

### 智能大纲提取
- [ ] 有内置目录的PDF正常
- [ ] 无内置目录的PDF使用智能提取
- [ ] 提取结果准确（准确率：___%）
- [ ] 问题：[填写或"无"]

### 热门概念过滤
- [ ] 无 table/figure/page
- [ ] 概念有意义
- [ ] 概念数量：___个
- [ ] 问题：[填写或"无"]

### 知识库选择
- [ ] 下拉菜单显示正常
- [ ] 切换知识库生效
- [ ] 搜索结果正确
- [ ] 问题：[填写或"无"]

### UI不遮挡
- [ ] 文档查看器覆盖概念面板
- [ ] 层级清晰
- [ ] 无视觉混乱
- [ ] 问题：[填写或"无"]

### 总体评价
- 通过: [ ] / 不通过: [ ]
- 评分：___/10
- 建议：[填写]
```

---

**测试完成后，如有问题请查看详细文档：**
- 📄 `OUTLINE_CONCEPT_IMPROVEMENTS.md` - 改进说明
- 🧪 `TEST_OUTLINE_CONCEPT.md` - 完整测试指南

