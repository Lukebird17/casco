# 页码修复总结

## 问题背景

用户报告两个问题：
1. **页码不准确**：图片的页码是估算的，不够精确
2. **UI 添加文件失败**：提示 `'EnhancedOCRProcessor' object has no attribute 'format_results_with_page_num'`

## 解决方案

### 1. 页码准确性修复

#### 问题原因
之前的页码是基于 markdown 文本位置估算的：
```python
estimated_page = len(text_before_img.split('\n\n')) // 10 + 1
```

#### 修复方案
从 MinerU 的 `_middle.json` 文件中提取准确的页码信息。

#### 实现细节

**新增方法 1：`_build_image_page_mapping`**
```python
def _build_image_page_mapping(self, output_dir: str) -> Dict[str, int]:
    """
    从 MinerU 的 middle.json 构建图片hash到页码的映射
    
    Returns:
        字典：{image_hash: page_number}
    """
```

**新增方法 2：`_extract_image_paths_from_block`**
```python
def _extract_image_paths_from_block(self, block: Dict, page_number: int, mapping: Dict):
    """
    递归从块中提取图片路径
    """
```

**修改方法：`_extract_image_info`**
- 在开始时调用 `_build_image_page_mapping()` 构建映射
- 使用映射查找图片的准确页码
- 如果映射中没有，才使用估算方法（向后兼容）

#### 验证结果

修复前（第28页的slab图）：
```
14. 1f2a5d7241a30fe1f0e7... -> 第 13 页  ❌ 错误
```

修复后：
```
14. 1f2a5d7241a30fe1f0e7... -> 第 28 页  ✅ 正确
```

---

### 2. UI 添加文件功能修复

#### 问题原因
`EnhancedOCRProcessor` 的 API 发生了变化：
- **旧版本**：`process_pdf()` + `format_results_with_page_num()`
- **新版本**：`process_file()` 直接返回包含 `content` 和 `images` 的字典

但 `dynamic_db_manager.py` 还在使用旧的 API。

#### 修复内容

**修复 1：`_load_with_ocr` 方法**

修复前：
```python
def _load_with_ocr(self, file_path: str, file_ext: str) -> str:
    if file_ext == '.pdf':
        results = self.ocr_processor.process_pdf(file_path)
        return self.ocr_processor.format_results_with_page_num(results, 'pdf')
    # ... 类似处理 docx, pptx
```

修复后：
```python
def _load_with_ocr(self, file_path: str, file_ext: str) -> str:
    """使用OCR加载文档（基于MinerU）"""
    if file_ext in ['.pdf', '.docx', '.pptx']:
        # 使用 MinerU 处理
        result = self.ocr_processor.process_file(file_path)
        if result and result.get('content'):
            return result['content']
        else:
            raise Exception(f"MinerU 处理失败: {file_path}")
    else:
        # 其他文件类型使用默认加载器
        return self.document_loader.load_file(file_path)
```

**修复 2：`add_file` 方法中的数据格式**

问题：`VectorStore.add_documents()` 的参数格式不正确。

修复前：
```python
chunks = self.text_splitter.split_text(content)  # List[str]
metadatas = [...]  # List[Dict]
self.vector_store.add_documents(chunks, metadatas)  # ❌ 两个参数
```

修复后：
```python
text_chunks = self.text_splitter.split_text(content)
chunks = [
    {
        'content': text,
        'filename': filename,
        'filepath': file_path,
        'filetype': file_ext[1:],
        'page_number': 0,
        'chunk_id': i,
        'course': course_name,
        # ...
    }
    for i, text in enumerate(text_chunks)
]
self.vector_store.add_documents(chunks)  # ✅ 一个参数，包含完整的字典列表
```

#### 验证结果

测试脚本运行成功：
```
✅ 成功添加 47 个文档块到向量数据库
  ✅ 已添加到向量库（课程: 测试课程）
```

---

## 影响范围

### 修改的文件

1. **enhanced_ocr.py**
   - 新增：`_build_image_page_mapping()` 方法
   - 新增：`_extract_image_paths_from_block()` 方法
   - 修改：`_extract_image_info()` 方法，使用准确的页码

2. **dynamic_db_manager.py**
   - 修改：`_load_with_ocr()` 方法，使用新的 MinerU API
   - 修改：`add_file()` 方法，调整数据格式以匹配 `VectorStore.add_documents()`

### 需要重新处理的数据

修复后需要重新处理数据以更新页码：
```bash
cd /home/honglianglu/hdd/rag-agent
rm -rf vector_db image_vector_db .mineru_cache
python process_data.py
```

---

## 测试验证

### 1. 页码准确性测试
```bash
python test_page_numbers.py
```

结果：第28页的slab图正确识别为第28页 ✅

### 2. UI 添加文件测试
```bash
python test_ui_add_file.py
```

结果：成功添加47个文档块 ✅

---

## 后续优化建议

1. **缓存更新机制**
   - 当前如果 `_middle.json` 不存在，会回退到估算方法
   - 可以考虑在缓存时也保存页码映射

2. **错误处理**
   - 增强 `_build_image_page_mapping` 的错误处理
   - 记录哪些图片使用了估算页码

3. **性能优化**
   - 页码映射可以缓存，避免每次都解析 `_middle.json`

---

## 兼容性说明

- ✅ 向后兼容：如果 `_middle.json` 不存在，自动回退到估算方法
- ✅ 缓存兼容：旧缓存中的数据会在下次处理时自动更新页码
- ✅ API 兼容：所有外部调用接口保持不变

---

**修复完成时间**: 2025-12-17  
**状态**: ✅ 已验证并部署

