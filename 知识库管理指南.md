# 知识库管理完整指南

## 概述

本系统现在支持完整的知识库管理功能，包括：
- ✅ 多知识库管理（创建、切换、删除）
- ✅ 在线文件上传并生成向量库
- ✅ PDF原文查看（不再是JSON文本）
- ✅ 文档大纲提取（支持PDF页码导航）
- ✅ 概念定位（支持PDF页面跳转）

---

## 一、知识库管理

### 1.1 访问知识库管理面板

1. 点击左侧边栏的 **数据库图标** (Database)
2. 知识库管理面板将从右侧滑出

### 1.2 创建新知识库

1. 点击 **"创建新知识库"** 按钮
2. 输入知识库名称（如：`物理课程`、`编程资料`）
3. 点击 **"确认创建"**
4. 系统会在项目根目录创建 `vector_db_<名称>` 文件夹

### 1.3 上传文件到知识库

**支持的文件格式：**
- PDF (.pdf)
- Word 文档 (.docx)
- 文本文件 (.txt)
- Markdown (.md)
- PowerPoint (.pptx)

**上传步骤：**
1. 确保已选中目标知识库（显示 "当前" 标签）
2. 在 "上传文件到当前知识库" 区域，点击 **"点击选择文件"**
3. 选择一个或多个文件（支持批量上传）
4. 查看选中的文件列表
5. 点击 **"上传并处理"** 按钮
6. 系统会自动：
   - 解析文件内容
   - 分割成文本块
   - 生成向量嵌入
   - 存储到向量数据库

**处理进度：**
```
📤 上传文件到知识库: 物理课程
   文件数量: 3
   ✅ 保存: 力学基础.pdf
   ✅ 保存: 电磁学.docx
   ✅ 保存: 笔记.txt
   ✅ 添加了 156 个文本块
   ✅ 添加了 12 张图片
```

### 1.4 切换知识库

1. 在知识库列表中，找到目标知识库
2. 点击 **"对钩"图标** (Check)
3. 系统会切换到该知识库
4. 后续的所有问答和检索都将使用该知识库

**当前知识库特征：**
- 带有 **"当前"** 蓝色标签
- 边框显示为蓝色
- 背景色为浅蓝色

### 1.5 删除知识库

⚠️ **注意：删除操作不可恢复！**

1. 找到要删除的知识库
2. 点击 **"垃圾桶"图标** (Trash)
3. 确认删除对话框
4. 系统会删除整个 `vector_db_<名称>` 文件夹

**限制：**
- 默认知识库（`vector_db`）不能删除
- 当前使用中的知识库被删除后，会自动切换到默认知识库

---

## 二、PDF原文查看

### 2.1 功能特点

- ✅ 显示PDF原始文件（保留格式、图片、表格）
- ✅ 支持页面导航
- ✅ 支持页码跳转
- ✅ 与文档大纲、概念定位联动

### 2.2 打开PDF文档

**方式一：从引用跳转**
1. 在对话中，点击消息下方的 **"引用文档"**
2. 点击 **"查看文档"** 按钮
3. 如果是PDF，会显示原文件

**方式二：从文档大纲跳转**
1. 打开 **"文档大纲"** 面板
2. 选择一个PDF文档
3. 点击章节标题，自动跳转到对应页面

**方式三：从概念定位跳转**
1. 打开 **"概念定位"** 面板
2. 搜索关键词（如：`内存分配`）
3. 点击搜索结果，自动跳转到PDF对应页面

### 2.3 PDF查看器功能

```
┌─────────────────────────────────────┐
│  📄 文档名称.pdf              ✕     │
├─────────────────────────────────────┤
│                                     │
│  [PDF原文内容]                      │
│  - 保留原始排版                      │
│  - 显示所有图片和表格                │
│  - 支持缩放、翻页                    │
│                                     │
└─────────────────────────────────────┘
```

---

## 三、文档大纲（PDF适配）

### 3.1 PDF大纲提取

系统会自动提取PDF的目录结构：

**情况1：PDF有内置目录**
```json
{
  "sections": [
    {"title": "第一章 引言", "level": 1, "page": 1},
    {"title": "1.1 背景", "level": 2, "page": 3},
    {"title": "1.2 目的", "level": 2, "page": 5},
    {"title": "第二章 方法", "level": 1, "page": 10}
  ]
}
```

**情况2：PDF无目录**
系统会生成基于页码的简单大纲：
```json
{
  "sections": [
    {"title": "第 1 页", "level": 1, "page": 1},
    {"title": "第 10 页", "level": 1, "page": 10},
    {"title": "第 20 页", "level": 1, "page": 20}
  ]
}
```

### 3.2 使用大纲导航PDF

1. 打开 **"文档大纲"** 面板
2. 从下拉菜单选择PDF文档
3. 系统会显示大纲结构
4. 点击任意章节标题
5. PDF查看器会自动跳转到对应页码

**页码跳转原理：**
```javascript
// 在iframe的URL中添加 #page=N 参数
<iframe src="document.pdf#page=5" />
```

---

## 四、概念定位（PDF适配）

### 4.1 搜索PDF中的概念

1. 打开 **"概念定位"** 面板
2. 输入关键词（如：`SLUB分配器`）
3. 点击 **"搜索"**
4. 系统会搜索所有文档，包括PDF

### 4.2 搜索结果格式

**PDF结果：**
```
┌───────────────────────────────────┐
│ 📖 OS-08 pm-manage.pdf           │
│ 第 15 页                          │
│                                   │
│ ...SLUB分配器是Linux内核中的      │
│ 一种高效内存分配机制...           │
│                                   │
│ ████████░░ 85%                    │
└───────────────────────────────────┘
```

**文本文件结果：**
```
┌───────────────────────────────────┐
│ 📖 notes.txt                      │
│ 第 42 行                          │
│                                   │
│ SLUB allocator implementation...  │
│                                   │
│ ██████████ 80%                    │
└───────────────────────────────────┘
```

### 4.3 点击跳转

点击任意搜索结果后：
- **PDF文件**：打开PDF查看器，跳转到第N页
- **文本文件**：打开文本查看器，滚动到第N行

---

## 五、后端API接口

### 5.1 知识库管理API

#### 获取所有知识库
```http
GET /api/knowledge-bases

Response:
{
  "success": true,
  "knowledge_bases": [
    {
      "id": "default",
      "name": "默认知识库",
      "path": "/path/to/vector_db",
      "size": 1.45,  // MB
      "is_current": true,
      "created_at": 1234567890
    },
    {
      "id": "physics",
      "name": "physics",
      "path": "/path/to/vector_db_physics",
      "size": 0.85,
      "is_current": false,
      "created_at": 1234567891
    }
  ],
  "count": 2
}
```

#### 创建新知识库
```http
POST /api/knowledge-bases
Content-Type: application/json

{
  "name": "physics",
  "files": []
}

Response:
{
  "success": true,
  "message": "知识库 'physics' 创建成功",
  "kb_id": "physics",
  "path": "/path/to/vector_db_physics"
}
```

#### 切换知识库
```http
POST /api/knowledge-bases/{kb_id}/switch

Response:
{
  "success": true,
  "message": "已切换到知识库: physics",
  "kb_id": "physics"
}
```

#### 删除知识库
```http
DELETE /api/knowledge-bases/{kb_id}

Response:
{
  "success": true,
  "message": "知识库 'physics' 已删除"
}
```

#### 上传文件到知识库
```http
POST /api/knowledge-bases/{kb_id}/upload
Content-Type: multipart/form-data

files: [file1, file2, ...]

Response:
{
  "success": true,
  "message": "成功处理 3 个文件",
  "documents_added": 15,
  "images_added": 3
}
```

### 5.2 文档API

#### 获取PDF原文件
```http
GET /api/documents/{filename}/raw

Response: [PDF Binary Data]
Content-Type: application/pdf
```

#### 获取文档大纲
```http
GET /api/documents/{filename}/outline

Response:
{
  "success": true,
  "outline": {
    "document": "OS-08.pdf",
    "total_pages": 50,
    "sections": [
      {"id": "outline-0", "title": "第一章", "level": 1, "page": 1},
      {"id": "outline-1", "title": "1.1 介绍", "level": 2, "page": 3}
    ]
  }
}
```

#### 在文档中搜索
```http
GET /api/documents/{filename}/search?query=关键词

Response:
{
  "success": true,
  "query": "SLUB",
  "document": "OS-08.pdf",
  "results": [
    {
      "page": 15,
      "line": 42,
      "context": "...SLUB分配器是Linux内核中的...",
      "match": "SLUB"
    }
  ],
  "total": 5
}
```

---

## 六、文件存储结构

```
rag-agent/
├── vector_db/                    # 默认知识库
│   ├── chroma.sqlite3            # ChromaDB数据库文件
│   └── ...
├── vector_db_physics/            # 物理课程知识库
│   ├── chroma.sqlite3
│   └── ...
├── vector_db_programming/        # 编程资料知识库
│   ├── chroma.sqlite3
│   └── ...
└── data/                         # 原始文档存储
    ├── OS-08 pm-manage.pdf
    ├── physics-notes.docx
    └── readme.txt
```

### 知识库命名规则

- **默认知识库**：`vector_db/`
- **自定义知识库**：`vector_db_<名称>/`

---

## 七、使用流程示例

### 示例1：创建物理课程知识库

```bash
1. 点击侧边栏 "数据库" 图标
2. 点击 "创建新知识库"
3. 输入名称: "physics"
4. 点击 "确认创建"
5. 在文件上传区域选择文件:
   - 力学.pdf
   - 电磁学.pdf
   - 热学.docx
6. 点击 "上传并处理 (3个文件)"
7. 等待处理完成
8. 系统显示: "✅ 添加了 245 个文本块"
```

### 示例2：搜索PDF中的概念

```bash
1. 点击侧边栏 "概念定位" 图标
2. 输入搜索词: "牛顿第二定律"
3. 点击 "搜索"
4. 查看搜索结果:
   ┌─────────────────────────────┐
   │ 📖 力学.pdf                 │
   │ 第 12 页                    │
   │ ...牛顿第二定律 F=ma...     │
   └─────────────────────────────┘
5. 点击结果
6. PDF查看器打开并跳转到第12页
7. 查看完整内容
```

### 示例3：通过大纲导航PDF

```bash
1. 点击侧边栏 "文档大纲" 图标
2. 从下拉菜单选择 "力学.pdf"
3. 系统显示大纲:
   - 第一章 运动学 (第1页)
   - 第二章 动力学 (第10页)
   - 2.1 牛顿定律 (第12页)
   - 2.2 功和能 (第20页)
4. 点击 "2.1 牛顿定律"
5. PDF查看器自动跳转到第12页
```

---

## 八、注意事项

### 8.1 文件大小限制

- 单个文件：建议 < 50MB
- 批量上传：建议总大小 < 200MB

### 8.2 向量库大小

- 每1000个文本块约占 5-10MB 磁盘空间
- 建议定期检查磁盘使用情况

### 8.3 PDF处理限制

- 扫描版PDF（纯图片）：无法提取文本，需OCR
- 加密PDF：需要先解密
- 大型PDF（>1000页）：处理时间较长

### 8.4 切换知识库的影响

切换知识库后：
- ✅ 新的问答使用新知识库
- ✅ 历史对话记录保留
- ❌ 无法查看其他知识库的文档引用

---

## 九、故障排查

### 问题1：上传文件后无法检索到

**可能原因：**
- 文件解析失败
- 向量嵌入生成失败
- 知识库未正确切换

**解决方法：**
```bash
# 1. 检查后端日志
cd /home/honglianglu/hdd/rag-agent/backend
tail -f nohup.out

# 2. 检查知识库文件
ls -lh /home/honglianglu/hdd/rag-agent/vector_db_*/

# 3. 重新创建知识库
# 在UI中删除并重新创建
```

### 问题2：PDF无法显示

**可能原因：**
- PDF文件损坏
- 浏览器不支持iframe嵌入PDF
- CORS跨域问题

**解决方法：**
```bash
# 1. 验证PDF文件完整性
cd /home/honglianglu/hdd/rag-agent/data
file *.pdf

# 2. 检查CORS配置
# backend/api.py 中已配置 CORSMiddleware

# 3. 使用Chrome/Edge浏览器（Firefox可能有兼容问题）
```

### 问题3：概念搜索返回0结果

**可能原因：**
- 关键词拼写错误
- PDF文本提取失败
- 搜索API未正确调用

**解决方法：**
```bash
# 1. 测试搜索API
curl "http://localhost:8000/api/documents/test.pdf/search?query=测试"

# 2. 检查PDF文本提取
python -c "
import PyPDF2
with open('data/test.pdf', 'rb') as f:
    reader = PyPDF2.PdfReader(f)
    print(reader.pages[0].extract_text())
"

# 3. 尝试更通用的关键词
```

---

## 十、性能优化建议

### 10.1 知识库规模

- **小型**（< 100个文档）：单个知识库即可
- **中型**（100-500个文档）：按主题分类创建2-3个知识库
- **大型**（> 500个文档）：细分领域，创建多个专用知识库

### 10.2 检索性能

```python
# 推荐配置（config.py）
CHUNK_SIZE = 500          # 文本块大小
CHUNK_OVERLAP = 100       # 重叠大小
RETRIEVAL_TOP_K = 5       # 检索数量
```

### 10.3 并发上传

批量上传大文件时：
- 建议每次上传 5-10 个文件
- 等待处理完成后再上传下一批
- 避免浏览器标签页超时

---

## 十一、未来扩展

### 计划中的功能

- [ ] 知识库导出/导入
- [ ] 知识库统计分析
- [ ] PDF标注和批注
- [ ] 多知识库联合检索
- [ ] 知识库版本控制
- [ ] 自动OCR支持（扫描版PDF）

---

## 附录：完整API清单

```
知识库管理：
  GET    /api/knowledge-bases              列出所有知识库
  POST   /api/knowledge-bases              创建新知识库
  POST   /api/knowledge-bases/{id}/switch  切换知识库
  DELETE /api/knowledge-bases/{id}         删除知识库
  POST   /api/knowledge-bases/{id}/upload  上传文件

文档管理：
  GET    /api/documents                    列出所有文档
  GET    /api/documents/{name}             获取文档内容（文本）
  GET    /api/documents/{name}/raw         获取PDF原文件
  GET    /api/documents/{name}/outline     获取文档大纲
  GET    /api/documents/{name}/search      搜索文档内容

问答系统：
  POST   /api/chat                         发送消息
  POST   /api/sessions                     创建会话
  GET    /api/sessions                     获取会话列表

工具系统：
  POST   /api/quiz/generate                生成测验
  GET    /api/flashcards                   获取闪卡列表
  POST   /api/snippets                     保存代码片段
  GET    /api/heatmap                      获取热力图
```

---

**文档版本：** v2.0  
**更新日期：** 2025年12月  
**作者：** RAG Agent Team



