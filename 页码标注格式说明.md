# 页码标注格式说明

## 📋 实现方案

**核心思想**：将页码信息**直接嵌入文本内容**，而不只是放在metadata中。

### 格式设计

```
【第5页】
这是第5页的实际内容...
包括所有文字、公式等...
【/第5页】
```

### 图片描述格式

```
【第5页·图片】(image_filename.png)
这张图展示了虚拟内存的页表结构...
【/图片】
```

---

## 🎯 优势

### 1. **内容即标注**
- 页码信息直接在文本里
- 向量化时页码会被编码进向量
- LLM直接看到页码信息

### 2. **不依赖Metadata**
- 即使metadata丢失，页码仍在内容中
- 检索结果直接显示页码
- 更加鲁棒

### 3. **LLM友好**
```
检索到的context示例：
==================
【第5页】
虚拟内存通过页表实现地址转换...
【/第5页】

【第6页】
页表包含虚拟页号到物理页号的映射...
【/第6页】
==================

LLM可以直接看到：
- 这段内容来自第5页
- 那段内容来自第6页
- 自动在回答中引用准确页码
```

### 4. **易于解析**
```python
import re

# 提取页码
page_nums = re.findall(r'【第(\d+)页】', text)

# 提取特定页的内容
page_5_content = re.search(r'【第5页】(.*?)【/第5页】', text, re.DOTALL)
```

---

## 💻 实现细节

### document_loader.py

#### 1. MinerU处理的PDF（准确页码）

```python
# 从content_list.json提取准确页码
for page_info in full_result['pages']:
    page_num = page_info['page_number']
    page_content = page_info['content']
    
    # 标注页码
    text_with_page = f"【第{page_num}页】\n{page_content}\n【/第{page_num}页】"
    
    pages.append({
        "text": text_with_page,
        "page_number": page_num,  # metadata中也保留
        "section": ""
    })
```

#### 2. PyPDF2处理的PDF（fallback）

```python
for page_num, page in enumerate(reader.pages, 1):
    text = page.extract_text()
    
    # 统一格式
    text_with_page = f"【第{page_num}页】\n{text}\n【/第{page_num}页】"
    
    pages.append({
        "text": text_with_page,
        "page_number": page_num,
        "section": f"第 {page_num} 页"
    })
```

#### 3. 图片描述

```python
# 将图片描述插入到对应页面
for img in page_images:
    desc = img.get('description', '[无描述]')
    img_filename = os.path.basename(img.get('image_path', ''))
    
    # 明确标注页码和图片
    img_block = f"\n\n【第{page_num}页·图片】({img_filename})\n{desc}\n【/图片】\n"
    
    # 插入到页面文本中
    page['text'] += img_block
```

---

## 📊 向量库中的存储

### 文本块示例

```python
{
    'content': """【第5页】
虚拟内存使用页表来实现地址转换。
每个进程都有自己的页表...
【/第5页】""",
    
    'filename': '操作系统.pdf',
    'page_number': 5,  # metadata中也保留，便于过滤
    'chunk_id': 0,
    ...
}
```

### 检索结果示例

```python
retrieved_docs = [
    {
        'content': '【第5页】\n虚拟内存...\n【/第5页】',
        'filename': '操作系统.pdf',
        'page_number': 5,
        'distance': 0.234
    },
    {
        'content': '【第6页·图片】(page_table.png)\n这张图展示了...\n【/图片】',
        'filename': '操作系统.pdf',
        'page_number': 6,
        'distance': 0.267
    }
]
```

---

## 🔄 完整工作流

```
1. 文档上传
   ↓
2. MinerU处理 → content_list.json (page_idx)
   ↓
3. enhanced_ocr.py 提取准确页码
   ↓
4. document_loader.py 标注页码到文本
   格式: 【第X页】...【/第X页】
   ↓
5. 图片描述生成 → 插入到对应页
   格式: 【第X页·图片】...【/图片】
   ↓
6. 文本分块（保留页码标注）
   ↓
7. 向量化（页码信息被编码）
   ↓
8. 检索时返回带页码的内容
   ↓
9. LLM生成答案时自动引用页码
```

---

## 🎯 LLM生成示例

**检索到的Context**:
```
【第5页】
虚拟内存通过页表实现地址转换...
【/第5页】

【第6页·图片】(page_table_structure.png)
这张图展示了二级页表的结构...
【/图片】
```

**LLM生成的答案**:
```
虚拟内存使用页表来实现地址转换（第5页）。
如图所示（第6页），二级页表采用...
```

**引用格式**:
- 自动提取：`【第5页】` → "第5页"
- 自动提取：`【第6页·图片】` → "第6页"
- 在答案中自然引用

---

## 🧪 测试结果

```bash
$ python test_page_format.py

🔍 页码标注检查
================================================================================
   ✅ 找到页码标记: 【第1页】

✅ 页码标注格式正确！
   格式: 【第X页】...内容...【/第X页】
```

---

## 📝 与旧版本的区别

### 旧版本（仅metadata）
```python
{
    'content': '虚拟内存通过页表...',  # ← 内容中没有页码信息
    'page_number': 5  # ← 只在metadata中
}
```

**问题**：
- LLM看不到页码
- 依赖metadata传递
- 引用不准确

### 新版本（内容+metadata）
```python
{
    'content': '【第5页】\n虚拟内存通过页表...\n【/第5页】',  # ← 内容中有页码
    'page_number': 5  # ← metadata中也保留
}
```

**优势**：
- ✅ LLM直接看到页码
- ✅ 双重保障（内容+metadata）
- ✅ 自动准确引用

---

## 🔧 配置选项

### 启用/禁用页码标注

```python
# document_loader.py

# 全局开关（如果需要）
ENABLE_PAGE_MARKERS = True

if ENABLE_PAGE_MARKERS:
    text_with_page = f"【第{page_num}页】\n{content}\n【/第{page_num}页】"
else:
    text_with_page = content  # 不标注
```

### 自定义标注格式

```python
# 可以修改标注符号
PAGE_START_MARKER = "【第{page}页】"
PAGE_END_MARKER = "【/第{page}页】"
IMAGE_MARKER = "【第{page}页·图片】"

# 或使用英文
PAGE_START_MARKER = "[Page {page}]"
PAGE_END_MARKER = "[/Page {page}]"
```

---

## ⚠️ 注意事项

1. **标注符号选择**：使用中文全角符号`【】`，避免与正文混淆
2. **图片标注**：明确标记`【第X页·图片】`，与普通文本区分
3. **页码一致性**：内容中的页码与metadata中的page_number必须一致
4. **分块处理**：文本分块时保留完整的页码标注
5. **正则匹配**：可以用正则轻松提取页码：`r'【第(\d+)页】'`

---

## 🚀 后续优化

1. **章节信息**：`【第5页·第2章·虚拟内存】`
2. **多级标注**：`【第5页·第2.3节】`
3. **表格标注**：`【第5页·表格】`
4. **公式标注**：`【第5页·公式3.2】`

---

生成时间: 2025-12-19
版本: v3.0


