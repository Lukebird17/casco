# 目录结构修复说明

## ✅ 已完成的修复

### 1. 目录结构整理
```
修复前：
  data/
    ├── 2_1_统计语言模型2025_秋.pdf    ❌ 文件在根目录
    └── default/                       (空目录)

修复后：
  data/
    └── default/                       ✅ 所有文件在子目录
        ├── 2_1_统计语言模型2025_秋.pdf
        ├── OS-07 vm-manage.pdf
        ├── OS-07 vm-manage_1766047648.pdf
        └── OS-08 pm-manage.pdf
```

### 2. 后端API修复

**修复的API：**
- `GET /api/knowledge-bases` - 扫描vector_db/下的子目录
- `POST /api/knowledge-bases` - 创建data/{kb_id}/和vector_db/{kb_id}/
- `DELETE /api/knowledge-bases/{kb_id}` - 同时删除两个目录
- `POST /api/knowledge-bases/{kb_id}/upload` - 文件保存到data/{kb_id}/
- `GET /api/knowledge-bases/{kb_id}/files` - 从data/{kb_id}/读取

**使用的辅助函数（来自config.py）：**
```python
get_kb_data_dir(kb_id)      # 获取 data/{kb_id}/
get_kb_vector_dir(kb_id)    # 获取 vector_db/{kb_id}/
ensure_kb_dirs(kb_id)       # 确保两个目录都存在
```

### 3. 前端状态管理修复

**KnowledgeBasePanel.jsx：**
- `loadKnowledgeBases(preserveCurrentId)` - 可选是否保留当前ID
- `handleSwitchKB()` - 切换后调用`loadKnowledgeBases(true)`保留选中状态
- `loadKBFiles()` - 添加日志和空值检查

**DocumentViewer.jsx：**
- `loadDocuments()` - 添加空值检查和日志
- 切换知识库后自动清除不存在的文档选择

---

## 🚀 测试步骤

### 步骤1: 重启后端
```bash
cd /home/honglianglu/hdd/rag-agent
./start_backend.sh
```

### 步骤2: 打开浏览器并刷新前端
```
按 Ctrl+Shift+R 强制刷新
```

### 步骤3: 测试知识库管理

#### 测试A: 查看默认知识库
1. 点击左侧栏 **数据库图标**
2. 应该看到"default知识库"（蓝色高亮，显示"当前"标签）
3. 滚动到底部，应该看到"当前知识库文件 (4)"
4. 展开文件列表，应该看到：
   - 2_1_统计语言模型2025_秋.pdf
   - OS-07 vm-manage.pdf
   - OS-07 vm-manage_1766047648.pdf
   - OS-08 pm-manage.pdf

#### 测试B: 创建新知识库
1. 点击 **[+ 创建新知识库]**
2. 输入 "test"
3. 点击 **[确认创建]**
4. 应该在列表中看到新的"test"知识库

#### 测试C: 切换知识库
1. 点击"test"知识库的 **[对钩图标]**
2. "test"应该变成蓝色高亮，显示"当前"标签
3. "default"的高亮应该消失
4. 底部文件列表应该显示 "当前知识库文件 (0)"（因为是空的）
5. **关键测试**：再次点击"default"的对钩图标
6. "default"应该重新高亮
7. 文件列表应该重新显示4个文件 ✅

#### 测试D: 上传文件到test知识库
1. 确保"test"被选中（蓝色高亮）
2. 选择一个小文件上传
3. 上传成功后，文件列表应该显示 "当前知识库文件 (1)"
4. 应该看到刚上传的文件
5. 切换到"default"，再切回"test"
6. 文件应该还在 ✅

### 步骤4: 测试文档查看器

#### 测试E: 选择知识库查看文档
1. 点击左侧栏 **文档查看图标**
2. 左上角应该有"选择知识库"下拉菜单
3. 默认选中"default知识库"
4. 左侧文档列表应该显示4个文件
5. 点击下拉菜单，选择"test"
6. 文档列表应该更新为test的文件
7. **关键测试**：再次选择"default"
8. 应该重新显示4个文件 ✅

#### 测试F: 查看文档（在线，不下载）
1. 在文档列表中点击任意PDF
2. 右侧应该显示PDF内容
3. **不应该触发下载**
4. 可以在浏览器中滚动、缩放、复制文本

---

## 🐛 如果还有问题

### 问题1: 切换后文件列表还是空的

**检查后端日志：**
```bash
# 在运行后端的终端查看输出
# 应该看到类似：
加载知识库文件: default
知识库 default 包含 4 个文件
```

**检查浏览器控制台：**
```javascript
// 按F12打开
// 应该看到类似：
加载知识库文件: default
知识库 default 包含 4 个文件
```

### 问题2: API返回404或500错误

**测试API：**
```bash
# 测试文件列表API
curl http://localhost:8000/api/knowledge-bases/default/files

# 应该返回：
{
  "success": true,
  "kb_id": "default",
  "files": [
    {"name": "...", "size": ..., "type": "pdf", ...}
  ],
  "count": 4
}
```

### 问题3: 文档点击后下载而不是在线查看

**检查浏览器：**
- 使用Chrome或Edge（Firefox可能有兼容问题）
- 清除浏览器缓存
- 检查URL是否包含`/raw`而不是`/download`

---

## 📊 修复对比

| 功能 | 修复前 | 修复后 |
|------|--------|--------|
| 文件存储 | ❌ data/ 根目录 | ✅ data/{kb_id}/ |
| 向量存储 | ❌ vector_db/ 或 vector_db_* | ✅ vector_db/{kb_id}/ |
| 切换高亮 | ❌ 不显示 | ✅ 蓝色高亮+标签 |
| 文件列表 | ❌ 切换后不更新 | ✅ 实时更新 |
| 文档查看 | ❌ 只能看data/ | ✅ 选择任意知识库 |
| 状态管理 | ❌ 切换后混乱 | ✅ preserveCurrentId |

---

## 🔍 调试技巧

### 1. 查看后端日志
```bash
# 运行后端时会输出：
加载知识库文件: {kb_id}
知识库 {kb_id} 包含 {count} 个文件
```

### 2. 查看前端日志
```javascript
// 打开浏览器控制台 (F12)
// 会看到：
加载文档列表: {kb_id}
知识库 {kb_id} 包含 {count} 个文件
```

### 3. 检查目录结构
```bash
cd /home/honglianglu/hdd/rag-agent

# 应该看到：
ls -la data/
  default/
  test/     (如果创建了)

ls -la vector_db/
  default/
  test/     (如果创建了)
```

### 4. 验证文件存在
```bash
ls -lh data/default/
# 应该列出所有文件

ls -lh data/test/
# 应该列出test中的文件
```

---

## ✅ 完成检查清单

测试完成后，确认以下都正常：
- [ ] 默认知识库显示4个文件
- [ ] 可以创建新知识库
- [ ] 切换知识库高亮正确显示
- [ ] 切换后文件列表正确更新
- [ ] 切回default后文件还在
- [ ] 文档查看器可以选择知识库
- [ ] 切换知识库后文档列表更新
- [ ] PDF在线查看（不下载）
- [ ] 可以删除文件
- [ ] 可以上传文件到指定知识库

---

## 🎯 核心修复点总结

1. **目录结构统一**：
   - data/{kb_id}/ - 原文件
   - vector_db/{kb_id}/ - 向量库

2. **API路径修复**：
   - 所有API都使用get_kb_data_dir()和get_kb_vector_dir()
   - 扫描vector_db/下的子目录而不是vector_db_*

3. **状态管理修复**：
   - loadKnowledgeBases(preserveCurrentId) - 可选保留当前ID
   - 切换后调用loadKnowledgeBases(true)
   - 所有加载函数添加空值检查

4. **日志和调试**：
   - 所有关键函数添加console.log
   - 后端打印详细的路径信息
   - 便于定位问题

现在系统应该可以正常工作了！🎉



