# RAG 问答智能体 - 完整使用说明

## 项目概述

基于 RAG-Anything 官方框架实现的智能问答系统，可以处理 data 目录下的 PDF 文档，并按照示例模板格式返回问答结果。

## 特点

✅ **严格遵循官方实现** - 完全按照 RAG-Anything 官方示例编写  
✅ **支持多模态处理** - 图像、表格、公式自动识别  
✅ **批量文档处理** - 支持递归处理整个目录  
✅ **标准输出格式** - 符合示例模板规范  
✅ **多种查询模式** - hybrid/local/global/naive 模式可选

---

## 快速开始

### 第一步：安装依赖

```bash
# 方式 1: 使用安装脚本（推荐）
./install_dependencies.sh

# 方式 2: 手动安装
cd /home/honglianglu/ssd/casco/RAG-Anything
pip install -e .
cd ..
pip install -r requirements_rag.txt
```

### 第二步：配置环境变量

```bash
# 1. 复制环境变量模板
cp env_example.sh env.sh

# 2. 编辑 env.sh，填入您的实际 API Key
nano env.sh  # 或使用其他编辑器

# 3. 加载环境变量
source env.sh
```

**环境变量说明：**

```bash
# LLM 配置（用于生成答案）
CLOUD_MODEL="deepseek-chat"              # 或 "gpt-4o-mini"
CLOUD_API_KEY="your_api_key"             # 您的 API Key
CLOUD_BASE_URL="https://api.deepseek.com/v1"  # API 地址

# Embedding 配置（用于向量化文档）
OPENAI_API_KEY="your_api_key"            # 您的 API Key
OPENAI_BASE_URL="https://api.openai.com/v1"
OPENAI_API_MODEL="text-embedding-3-large"
```

### 第三步：运行程序

```bash
# 方式 1: 交互式启动（推荐新手）
python quick_start.py

# 方式 2: 直接运行完整流程
python rag_qa_agent.py

# 方式 3: 运行示例程序
python simple_qa_example.py
```

---

## 详细使用说明

### 1. 使用快速启动脚本（推荐）

```bash
python quick_start.py
```

**菜单选项：**

- `1` - 处理单个文档：选择一个 PDF 文件进行处理
- `2` - 批量处理事故报告：处理 `data/事故报告/` 目录下的所有 PDF
- `3` - 批量处理所有文档：处理 `data/` 目录下的所有 PDF（耗时较长）
- `4` - 仅查询：在已处理的文档中进行查询
- `5` - 交互式问答：进入命令行交互模式
- `6` - 运行完整示例：处理文档 + 执行示例查询 + 保存结果

### 2. 编程使用

#### 基础示例

```python
import asyncio
from rag_qa_agent import RAGQAAgent

async def basic_example():
    # 初始化智能体
    agent = RAGQAAgent(
        working_dir="./rag_storage",
        parser="mineru",
        parse_method="auto"
    )
    
    # 处理文档
    await agent.process_document(
        file_path="/path/to/your/document.pdf",
        output_dir="./output"
    )
    
    # 查询
    result = await agent.query(
        "你的问题？",
        mode="hybrid"
    )
    
    print(result['answer'])

asyncio.run(basic_example())
```

#### 批量处理示例

```python
import asyncio
from rag_qa_agent import RAGQAAgent

async def batch_example():
    agent = RAGQAAgent()
    
    # 批量处理文档
    await agent.process_folder(
        folder_path="/home/honglianglu/ssd/casco/data",
        output_dir="./output",
        file_extensions=[".pdf"],
        recursive=True,
        max_workers=4
    )
    
    # 批量查询
    questions = [
        "问题1？",
        "问题2？",
        "问题3？"
    ]
    
    results = await agent.query_batch(questions)
    
    # 保存结果（按示例模板格式）
    agent.save_results(results, "results.json")

asyncio.run(batch_example())
```

---

## 核心功能说明

### 1. 文档处理

#### 处理单个文档

```python
await agent.process_document(
    file_path="document.pdf",
    output_dir="./output",
    parse_method="auto"  # 可选: auto, ocr, txt
)
```

#### 批量处理文档

```python
await agent.process_folder(
    folder_path="./data",
    output_dir="./output",
    file_extensions=[".pdf", ".docx"],
    recursive=True,      # 递归处理子目录
    max_workers=4        # 并行处理数量
)
```

**支持的文档格式：**
- PDF 文档
- Office 文档（.doc, .docx, .ppt, .pptx, .xls, .xlsx）
- 图像文件（.jpg, .png, .bmp, .tiff, .gif, .webp）
- 文本文件（.txt, .md）

### 2. 查询功能

#### 单个查询

```python
result = await agent.query(
    question="你的问题？",
    mode="hybrid"  # hybrid/local/global/naive
)

print(result['question'])  # 问题
print(result['answer'])    # 答案
```

#### 批量查询

```python
results = await agent.query_batch(
    questions=["问题1", "问题2", "问题3"],
    mode="hybrid"
)
```

**查询模式说明：**

| 模式 | 说明 | 适用场景 |
|------|------|----------|
| `hybrid` | 混合检索（推荐） | 平衡准确性和全面性 |
| `local` | 局部检索 | 关注局部细节信息 |
| `global` | 全局检索 | 需要整体概述 |
| `naive` | 简单向量检索 | 快速简单查询 |

### 3. 结果保存

```python
agent.save_results(
    results=results,
    output_file="qa_results.json"
)
```

**输出格式（符合示例模板）：**

```json
{
  "items": [
    {
      "question": "这里填写提问内容",
      "retrieved_contexts": [
        "召回的上下文1",
        "召回的上下文2"
      ],
      "answer": "基于召回内容生成的回答"
    }
  ]
}
```

---

## 文件结构

```
/home/honglianglu/ssd/casco/
│
├── data/                              # 📁 PDF 文档目录
│   ├── 事故报告/                      # 事故调查报告
│   ├── 技术报告/                      # 技术文档
│   ├── 标准规范/                      # 标准规范文档
│   └── 行业报告/                      # 行业分析报告
│
├── RAG-Anything/                      # 📦 RAG-Anything 框架
│
├── rag_storage/                       # 💾 RAG 存储目录（自动创建）
│   ├── kv_store_*.json               # 键值存储
│   ├── vdb_entities_*.json           # 实体向量数据库
│   └── graph_*.json                  # 知识图谱
│
├── output/                            # 📄 文档解析输出（自动创建）
│   └── *.md                          # 解析后的 Markdown 文件
│
├── rag_qa_agent.py                   # 🎯 主程序
├── quick_start.py                    # 🚀 快速启动脚本
├── simple_qa_example.py              # 📝 使用示例
│
├── config_template.py                # ⚙️  配置模板
├── env_example.sh                    # 🔑 环境变量示例
├── install_dependencies.sh           # 📦 安装脚本
├── requirements_rag.txt              # 📋 依赖列表
│
├── README_RAG_QA.md                  # 📖 详细文档
├── 使用说明.md                        # 📖 本文档
│
└── qa_results.json                   # 📊 查询结果（自动生成）
```

---

## 官方实现对照

本实现严格遵循 RAG-Anything 官方示例，主要包括：

### 1. LLM 函数定义 ✅

```python
def llm_model_func(prompt, system_prompt=None, history_messages=[], **kwargs):
    return openai_complete_if_cache(
        "gpt-4o-mini",
        prompt,
        system_prompt=system_prompt,
        history_messages=history_messages,
        api_key=api_key,
        base_url=base_url,
        **kwargs,
    )
```

### 2. Vision 函数定义 ✅

```python
def vision_model_func(
    prompt, system_prompt=None, history_messages=[], 
    image_data=None, messages=None, **kwargs
):
    # 支持 messages 格式（多模态 VLM）
    if messages:
        return openai_complete_if_cache(...)
    # 支持 image_data 格式（单图片）
    elif image_data:
        return openai_complete_if_cache(...)
    # 纯文本格式
    else:
        return llm_model_func(...)
```

### 3. Embedding 函数 ✅

```python
embedding_func = EmbeddingFunc(
    embedding_dim=3072,
    max_token_size=8192,
    func=lambda texts: openai_embed(
        texts,
        model="text-embedding-3-large",
        api_key=api_key,
        base_url=base_url,
    ),
)
```

### 4. RAGAnything 配置 ✅

```python
config = RAGAnythingConfig(
    working_dir="./rag_storage",
    parser="mineru",
    parse_method="auto",
    enable_image_processing=True,
    enable_table_processing=True,
    enable_equation_processing=True,
)
```

### 5. 文档处理 ✅

```python
# 单个文档
await rag.process_document_complete(
    file_path="path/to/your/document.pdf",
    output_dir="./output",
    parse_method="auto"
)

# 批量处理
await rag.process_folder_complete(
    folder_path="./documents",
    output_dir="./output",
    file_extensions=[".pdf", ".docx", ".pptx"],
    recursive=True,
    max_workers=4
)
```

### 6. 查询方法 ✅

```python
# 纯文本查询
result = await rag.aquery("问题", mode="hybrid")

# VLM 增强查询（自动启用）
result = await rag.aquery("关于图片的问题", mode="hybrid")
```

---

## 常见问题

### Q1: 找不到 API Key

**错误：**
```
ValueError: 未找到 API Key，请设置 CLOUD_API_KEY 或 OPENAI_API_KEY 环境变量
```

**解决方案：**
```bash
# 设置环境变量
export CLOUD_API_KEY="your_api_key"
export OPENAI_API_KEY="your_api_key"

# 或者使用 source 加载
source env.sh
```

### Q2: 文档处理失败

**错误：**
```
Error processing document: ...
```

**可能原因：**
1. 文档路径不正确
2. MinerU 未正确安装
3. 文档格式不支持

**解决方案：**
```bash
# 1. 检查文档路径
ls -lh /path/to/document.pdf

# 2. 重新安装 MinerU
cd RAG-Anything
pip install -e .

# 3. 尝试不同的解析方法
parse_method="ocr"  # 或 "txt"
```

### Q3: 查询无结果或结果不相关

**可能原因：**
1. 文档未处理完成
2. 问题描述不清晰
3. 查询模式不适合

**解决方案：**
```python
# 1. 确认文档已处理
# 检查 rag_storage 目录是否有数据

# 2. 改进问题描述
question = "详细描述你想了解的内容"

# 3. 尝试不同查询模式
result = await agent.query(question, mode="global")  # 或 "local"
```

### Q4: 处理速度慢

**优化建议：**

1. **调整并行处理数：**
```python
max_workers=2  # 根据 CPU 核心数调整
```

2. **使用更快的解析方法：**
```python
parse_method="txt"  # 纯文本模式更快
```

3. **选择性处理文档：**
```python
file_extensions=[".pdf"]  # 只处理 PDF
recursive=False           # 不递归子目录
```

### Q5: 内存不足

**解决方案：**

1. **减少并行处理数：**
```python
max_workers=1
```

2. **分批处理文档：**
```python
# 分批处理，每批处理少量文档
for folder in ["事故报告", "技术报告"]:
    await agent.process_folder(f"data/{folder}", max_workers=1)
```

---

## 性能建议

### 文档处理

| 文档数量 | 推荐配置 | 预计时间 |
|---------|---------|---------|
| 1-10 个 | max_workers=2 | 几分钟 |
| 10-50 个 | max_workers=2-4 | 10-30 分钟 |
| 50+ 个 | max_workers=4, 分批处理 | 1+ 小时 |

### 查询性能

| 查询模式 | 速度 | 准确性 | 适用场景 |
|---------|------|--------|---------|
| naive | 最快 | 中等 | 快速查找 |
| local | 快 | 较高 | 局部信息 |
| global | 慢 | 高 | 全局概览 |
| hybrid | 中等 | 最高 | 一般用途（推荐） |

---

## 进阶用法

### 1. 自定义配置

```python
from raganything import RAGAnythingConfig

config = RAGAnythingConfig(
    working_dir="./custom_rag_storage",
    parser="docling",  # 使用 Docling 解析器
    parse_method="ocr",
    enable_image_processing=True,
    enable_table_processing=True,
    enable_equation_processing=False,  # 禁用公式处理
)

agent = RAGQAAgent(config=config)
```

### 2. 使用不同的 LLM

```python
# 在环境变量中修改
export CLOUD_MODEL="gpt-4o"  # 使用 GPT-4
export CLOUD_MODEL="claude-3-opus"  # 使用 Claude
```

### 3. 调整 Embedding 维度

```python
# 修改 rag_qa_agent.py 中的配置
embedding_func = EmbeddingFunc(
    embedding_dim=1536,  # 修改为您的模型维度
    max_token_size=8192,
    func=...
)
```

---

## 技术支持

### 相关文档

- [RAG-Anything 官方文档](../RAG-Anything/README_zh.md)
- [LightRAG 文档](https://github.com/HKUDS/LightRAG)
- [详细 API 文档](README_RAG_QA.md)

### 日志调试

```python
# 启用详细日志
import logging
logging.basicConfig(level=logging.DEBUG)
```

### 问题反馈

如遇到问题，请检查：
1. 环境变量是否正确配置
2. 依赖是否完整安装
3. API Key 是否有效
4. 文档路径是否正确

---

## 版本信息

- **创建日期**: 2025-11-21
- **Python 版本**: 3.8+
- **RAG-Anything**: 最新版
- **适用场景**: 文档问答、知识库构建

---

## 许可证

本项目遵循 RAG-Anything 的许可证要求。

---

## 附录：完整工作流程

```
1. 安装依赖
   ./install_dependencies.sh
   
2. 配置环境
   cp env_example.sh env.sh
   编辑 env.sh
   source env.sh
   
3. 处理文档
   python quick_start.py
   选择: 2 (批量处理事故报告)
   
4. 执行查询
   python quick_start.py
   选择: 4 (仅查询)
   
5. 查看结果
   cat qa_results.json
```

---

**祝使用愉快！** 🎉

