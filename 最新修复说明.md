# 最新修复说明（2025-12-18）

## ✅ 已完成的修复

### 1. PDF文件名编码问题 ✨

**问题**：中文PDF文件名导致 `'latin-1' codec can't encode` 错误

**修复**：
- 使用RFC 5987标准的URL编码处理文件名
- 修改所有`FileResponse`的`Content-Disposition`头
- 格式：`filename*=UTF-8''encoded_filename`

**影响文件**：
- `backend/api.py` - 两处FileResponse

**测试**：
```bash
# 测试中文文件名的PDF
curl "http://localhost:8000/api/knowledge-bases/default/documents/2_1_统计语言模型2025_秋.pdf/raw"
```

---

### 2. PPTX/DOCX 处理策略改进 🔄

**问题**：MinerU只支持PDF，不支持PPTX/DOCX

**解决方案**：
1. 使用LibreOffice将PPTX/DOCX转换为PDF
2. 用MinerU处理转换后的PDF
3. 如果转换失败，回退到基础解析器

**修改的文件**：
- `document_loader.py`:
  - 添加 `convert_to_pdf()` 方法
  - 重构 `load_pptx()` - 先转PDF再用MinerU
  - 重构 `load_docx()` - 先转PDF再用MinerU
  - 在临时目录进行转换，处理完自动清理

**转换流程**：
```
PPTX/DOCX → LibreOffice → PDF → MinerU → Markdown
             (转换)              (提取)
```

**依赖安装**：
```bash
# 安装LibreOffice（用于文档转换）
./安装LibreOffice.sh

# 或手动安装
sudo apt-get install libreoffice-writer libreoffice-impress
```

**测试**：
```python
from document_loader import DocumentLoader

loader = DocumentLoader()

# 测试PPTX
slides = loader.load_pptx("test.pptx")  # 自动转PDF → MinerU
print(f"提取到 {len(slides)} 页")

# 测试DOCX
content = loader.load_docx("test.docx")  # 自动转PDF → MinerU
print(f"提取文本: {len(content)} 字符")
```

---

### 3. Markdown文件支持 📝

**新增功能**：
- 向量化：支持.md文件的加载和向量化
- 查看：在文档查看器中以文本方式显示

**修改的文件**：
- `document_loader.py`:
  - 添加 `load_md()` 方法
  - 在 `load_document()` 中添加.md处理
  - 更新 `supported_formats` 包含 ".md"

**测试**：
```python
# 测试Markdown加载
loader = DocumentLoader()
content = loader.load_md("README.md")
print(content)

# 向量化测试
docs, images = loader.load_document("test.md")
print(f"文档块: {len(docs)}")
```

**前端支持**：
- `DocumentViewer.jsx` - 已支持.md文件（作为text类型）
- `KnowledgeBasePanel.jsx` - 文件图标识别.md

---

### 4. 知识库切换优化 🔄

**问题**：上传文件后自动切换回default知识库

**根本原因**：
- 上传成功后调用 `loadKnowledgeBases()` 
- 该函数会根据后端的 `is_current` 重置 `currentKBId`

**修复**：
```javascript
// 修改前
loadKnowledgeBases();  // ❌ 会重置currentKBId

// 修改后
loadKnowledgeBases(true);  // ✅ 保持currentKBId
```

**修改的文件**：
- `frontend/src/components/KnowledgeBasePanel.jsx`
  - `handleUploadFiles()` - 上传成功后传递 `true` 参数

**测试流程**：
1. 切换到"test"知识库
2. 上传一个文件
3. 上传成功后，应该仍然停留在"test"知识库 ✅
4. 文件列表自动更新显示新文件 ✅

---

### 5. PPTX查看器优化 🖼️

**问题**：显示 "需要安装LibreOffice" 的提示

**修复**：
- 移除提示信息（因为现在PPTX会自动转PDF处理）
- 改进HTML样式，更好地展示文本内容

**修改的文件**：
- `backend/api.py` - `/api/knowledge-bases/{kb_id}/documents/{filename}/preview/pptx`

**显示效果**：
```
┌────────────────────────────────┐
│       filename.pptx            │
│     第 1 页 / 共 10 页         │
├────────────────────────────────┤
│                                │
│  幻灯片内容（纯文本）          │
│                                │
│  • 标题                        │
│  • 要点1                       │
│  • 要点2                       │
│                                │
└────────────────────────────────┘
```

---

### 6. 知识库切换API修复 🔧

**问题**：新创建的知识库无法切换

**根本原因**：
- `/api/knowledge-bases/{kb_id}/switch` 使用旧的路径结构
- 查找 `vector_db_{kb_id}` 而不是 `vector_db/{kb_id}`

**修复**：
```python
# 修改前
kb_path = os.path.join(project_root, f"vector_db_{kb_id}")  # ❌

# 修改后
from config import get_kb_vector_dir
kb_vector_path = get_kb_vector_dir(kb_id)  # ✅ vector_db/{kb_id}/
```

**修改的文件**：
- `backend/api.py`:
  - `switch_knowledge_base()` - 使用新的目录结构
  - `upload_to_knowledge_base()` - 使用新的目录结构

**测试**：
```bash
# 创建新知识库
curl -X POST http://localhost:8000/api/knowledge-bases \
  -H "Content-Type: application/json" \
  -d '{"name": "test"}'

# 切换到新知识库（应该成功）
curl -X POST http://localhost:8000/api/knowledge-bases/test/switch
```

---

## 📦 新增文件

### 1. `安装LibreOffice.sh`
```bash
#!/bin/bash
# 安装LibreOffice用于文档转换
sudo apt-get update
sudo apt-get install -y libreoffice-writer libreoffice-impress
```

### 2. `test_kb_switch.sh`
- 测试知识库切换功能
- 验证创建、切换、文件上传等操作

### 3. `fix_directory_structure.sh`
- 修复目录结构
- 将 `data/` 下的文件移动到 `data/default/`

---

## 🗂️ 目录结构（最终版本）

```
/home/honglianglu/hdd/rag-agent/
├── data/
│   ├── default/           ← 默认知识库的原文件
│   │   ├── file1.pdf
│   │   └── file2.pptx
│   ├── test/              ← test知识库的原文件
│   │   └── uploaded.pdf
│   └── nlp/               ← nlp知识库的原文件
│
├── vector_db/
│   ├── default/           ← 默认知识库的向量
│   │   ├── chroma.sqlite3
│   │   └── ...
│   ├── test/              ← test知识库的向量
│   └── nlp/               ← nlp知识库的向量
│
├── backend/
│   └── api.py             ← 所有API使用 get_kb_data_dir/get_kb_vector_dir
│
└── config.py              ← 定义路径辅助函数
```

---

## 🧪 完整测试流程

### 步骤1: 重启后端
```bash
cd /home/honglianglu/hdd/rag-agent
./start_backend.sh
```

### 步骤2: 测试文件名编码
1. 打开文档查看器
2. 选择 "2_1_统计语言模型2025_秋.pdf"
3. 应该能正常在线显示，不报编码错误 ✅

### 步骤3: 测试知识库切换
1. 打开知识库管理面板
2. 创建新知识库 "test_new"
3. 点击切换按钮
4. 应该成功切换，显示蓝色高亮 ✅

### 步骤4: 测试文件上传（保持选中）
1. 确保 "test_new" 被选中
2. 上传一个PDF文件
3. 上传成功后
   - 仍然停留在 "test_new" ✅
   - 文件列表显示新上传的文件 ✅

### 步骤5: 测试PPTX上传（转PDF处理）
1. 上传一个PPTX文件
2. 后端应该输出：
   ```
   🔄 转换为PDF: test.pptx
   ✅ 转换成功: test.pdf
   ✅ MinerU处理完成
   ```
3. 文件应该被成功向量化 ✅

### 步骤6: 测试MD文件
1. 上传一个 README.md
2. 在文档查看器中打开
3. 应该显示为纯文本 ✅

---

## 🔍 调试技巧

### 1. 查看后端日志
```bash
# 运行后端时会输出详细日志：
🔄 转换为PDF: test.pptx
✅ 转换成功: test.pdf
📤 上传文件到知识库: test
   数据目录: /home/honglianglu/hdd/rag-agent/data/test
   向量库目录: /home/honglianglu/hdd/rag-agent/vector_db/test
```

### 2. 检查LibreOffice安装
```bash
which libreoffice
# 应该输出: /usr/bin/libreoffice

libreoffice --version
# 应该输出版本信息
```

### 3. 手动测试文档转换
```bash
# 测试PPTX转PDF
libreoffice --headless --convert-to pdf --outdir /tmp test.pptx

# 检查生成的PDF
ls -lh /tmp/test.pdf
```

### 4. 验证目录结构
```bash
# 应该看到正确的结构
ls -la data/
  default/
  test/
  nlp/

ls -la vector_db/
  default/
  test/
  nlp/
```

---

## ⚠️ 注意事项

### 1. LibreOffice依赖（可选）
- **推荐安装**（如果有sudo权限）：`./安装LibreOffice.sh`
- **如果没有安装或无sudo权限**：
  - PPTX/DOCX会自动回退到基础解析器（只提取文本）
  - 或者手动将文件转换为PDF后上传
  - 详见：**无sudo权限使用指南.md**

### 2. 转换性能
- LibreOffice转换需要时间（5-30秒）
- 大文件可能更慢
- 转换在临时目录进行，自动清理

### 3. 文件格式支持
| 格式   | 向量化 | 查看 | 方法                        |
|--------|--------|------|----------------------------|
| PDF    | ✅     | ✅   | MinerU直接处理             |
| PPTX   | ✅     | ✅   | 转PDF → MinerU             |
| DOCX   | ✅     | ✅   | 转PDF → MinerU             |
| TXT    | ✅     | ✅   | 直接读取                   |
| MD     | ✅     | ✅   | 直接读取                   |
| 图片   | ✅     | ✅   | CLIP向量化                 |

### 4. 错误处理
```python
# PPTX处理流程（带回退）
try:
    pdf = convert_to_pdf(pptx_file)  # 尝试转换
    result = mineru.process(pdf)      # 尝试MinerU
except:
    result = python_pptx.extract(pptx_file)  # 回退到基础解析器
```

---

## 🎯 核心改进点

### 1. 编码处理 ✅
- 所有文件名使用UTF-8 URL编码
- 符合RFC 5987标准
- 支持任意Unicode字符

### 2. 文档转换 ✅
- PPTX/DOCX → PDF → MinerU
- 完整的OCR和图片提取
- 自动回退机制

### 3. 文件支持 ✅
- 新增Markdown支持
- 统一的向量化流程
- 一致的查看体验

### 4. 状态管理 ✅
- 知识库切换不混乱
- 上传后保持选中状态
- 实时更新文件列表

### 5. 用户体验 ✅
- 移除不必要的提示
- 统一的错误处理
- 详细的日志输出

---

## 📊 性能对比

| 操作       | 修复前             | 修复后              |
|------------|-------------------|---------------------|
| PDF查看    | ❌ 中文文件名报错 | ✅ 正常显示         |
| PPTX向量化 | ❌ 仅文本         | ✅ 完整OCR+图片     |
| DOCX向量化 | ❌ 仅文本         | ✅ 完整OCR+图片     |
| MD支持     | ❌ 不支持         | ✅ 完整支持         |
| 知识库切换 | ❌ 上传后混乱     | ✅ 状态保持         |
| 切换API    | ❌ 找不到路径     | ✅ 使用新结构       |

---

## ✅ 完成检查清单

测试完成后，确认以下都正常：
- [ ] 中文PDF文件名能正常查看
- [ ] 可以创建并切换到新知识库
- [ ] 上传文件后停留在当前知识库
- [ ] PPTX文件能成功向量化（带OCR）
- [ ] DOCX文件能成功向量化（带OCR）
- [ ] MD文件能查看和向量化
- [ ] PPTX查看器不再显示LibreOffice提示
- [ ] 所有知识库的数据存储在正确位置

---

## 🚀 下一步

1. **安装LibreOffice**（如果还没有）：
   ```bash
   ./安装LibreOffice.sh
   ```

2. **重启后端**：
   ```bash
   ./start_backend.sh
   ```

3. **测试**：
   - 上传PPTX文件测试转换
   - 上传中文文件名的PDF测试编码
   - 切换知识库测试状态保持

4. **享受新功能**！🎉

现在系统应该完全正常工作了！

