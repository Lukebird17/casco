# 📊 向量库管理指南

## 🎯 统一路径配置

所有脚本现在都使用 **绝对路径**，确保在任何目录运行都能正确访问向量库：

```python
# config.py 中的配置
PROJECT_ROOT = /home/honglianglu/hdd/rag-agent
VECTOR_DB_PATH = /home/honglianglu/hdd/rag-agent/vector_db
DATA_DIR = /home/honglianglu/hdd/rag-agent/data
```

## 📝 生成向量库

### 1. 准备数据
将 PDF、PPTX、DOCX 或 TXT 文件放入 `data/` 目录：

```bash
cd /home/honglianglu/hdd/rag-agent
ls data/
```

### 2. 运行生成脚本

```bash
cd /home/honglianglu/hdd/rag-agent
conda activate rag
python process_data.py
```

**输出示例**：
```
============================================================
📚 加载文档...
============================================================

✅ 加载完成:
   - 文档: 10 个
   - 图片: 5 张

============================================================
📝 处理文本数据...
============================================================
✅ 已添加 94 个文档块

============================================================
🖼️  处理图片数据...
============================================================
✅ 已添加 5 张图片

============================================================
📊 数据处理完成！
============================================================
✅ 文本文档块: 94
✅ 图片数量: 5
```

### 3. 验证向量库

```bash
# 检查数据库文件
ls -lh /home/honglianglu/hdd/rag-agent/vector_db/chroma.sqlite3

# 应该看到类似：
# -rw-r--r-- 1 honglianglu honglianglu 1.4M Dec 18 12:07 chroma.sqlite3
```

## 🔄 更新向量库

如果添加了新文档，重新运行 `process_data.py` 即可：

```bash
cd /home/honglianglu/hdd/rag-agent
python process_data.py
```

**注意**：`process_data.py` 会清空现有数据库并重新生成，所以确保 `data/` 目录包含所有需要的文档。

## 🚀 启动应用

### 方式1：使用启动脚本（推荐）

```bash
cd /home/honglianglu/hdd/rag-agent

# 启动后端
./start_backend.sh

# 在另一个终端启动前端
./start_frontend.sh
```

### 方式2：手动启动

```bash
# 终端1：启动后端
cd /home/honglianglu/hdd/rag-agent
conda activate rag
cd backend
uvicorn api:app --host 0.0.0.0 --port 8000 --reload

# 终端2：启动前端
cd /home/honglianglu/hdd/rag-agent
conda activate rag
cd frontend
npm run dev
```

## 🔍 验证数据库路径

启动后端时，会显示使用的数据库路径：

```
🚀 正在初始化 RAG Agent...
📂 数据库路径: /home/honglianglu/hdd/rag-agent/vector_db
✅ 数据库文件存在: 1.40 MB  ← 确认大小正确
✅ RAG Agent 和所有工具初始化成功
```

如果显示 `0.16 MB` 或其他很小的大小，说明数据库是空的，需要重新运行 `process_data.py`。

## 🛠️ 常见问题

### Q: 为什么之前会出现路径问题？
A: 旧版本使用相对路径 `./vector_db`，导致在不同目录运行时访问不同的数据库。现在统一使用绝对路径。

### Q: 如何清空向量库？
A: 删除 `vector_db/` 目录，然后重新运行 `python process_data.py`：
```bash
rm -rf /home/honglianglu/hdd/rag-agent/vector_db
python process_data.py
```

### Q: 如何检查向量库中有多少数据？
A: 运行测试脚本：
```bash
python test_simple.py
```

## ✅ 最佳实践

1. **始终在项目根目录运行脚本**：
   ```bash
   cd /home/honglianglu/hdd/rag-agent
   python process_data.py
   ```

2. **使用绝对路径**（已在 `config.py` 中配置）

3. **定期备份向量库**：
   ```bash
   tar -czf vector_db_backup_$(date +%Y%m%d).tar.gz vector_db/
   ```

4. **验证数据库大小**：生成后应该至少有 1MB 以上（取决于文档数量）

---

📅 更新时间：2025-12-18  
🎯 配置文件：`config.py`  
🔧 生成脚本：`process_data.py`




